# 零食小程序商城 - 数据库设计

## 一、数据库概述

### 1.1 基本信息
- **数据库类型**：MySQL 8.0+
- **字符集**：utf8mb4
- **排序规则**：utf8mb4_unicode_ci
- **存储引擎**：InnoDB
- **时区**：Asia/Shanghai

### 1.2 命名规范
- 表名：小写 + 下划线，复数形式（如：users, products）
- 字段名：小写 + 下划线（如：user_id, created_at）
- 索引名：idx_表名_字段名
- 外键名：fk_表名_字段名

---

## 二、表结构设计

### 2.1 用户表（users）

**功能**：存储用户基本信息

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 用户ID（主键） |
| openid | VARCHAR | 100 | NO | - | 微信OpenID（唯一） |
| unionid | VARCHAR | 100 | YES | NULL | 微信UnionID |
| nickname | VARCHAR | 50 | YES | NULL | 昵称 |
| avatar | VARCHAR | 255 | YES | NULL | 头像URL |
| phone | VARCHAR | 11 | YES | NULL | 手机号 |
| gender | TINYINT | - | YES | 0 | 性别（0:未知 1:男 2:女） |
| level | TINYINT | - | NO | 1 | 会员等级（1-5） |
| points | INT | - | NO | 0 | 积分余额 |
| balance | DECIMAL | 10,2 | NO | 0.00 | 账户余额 |
| status | TINYINT | - | NO | 1 | 状态（0:禁用 1:正常） |
| last_login_at | DATETIME | - | YES | NULL | 最后登录时间 |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT | 更新时间 |
| deleted_at | DATETIME | - | YES | NULL | 软删除时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `idx_openid` (`openid`)
- KEY `idx_phone` (`phone`)
- KEY `idx_created_at` (`created_at`)

---

### 2.2 收货地址表（addresses）

**功能**：存储用户收货地址

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 地址ID（主键） |
| user_id | BIGINT | - | NO | - | 用户ID |
| consignee | VARCHAR | 50 | NO | - | 收货人姓名 |
| phone | VARCHAR | 11 | NO | - | 收货人电话 |
| province | VARCHAR | 50 | NO | - | 省份 |
| city | VARCHAR | 50 | NO | - | 城市 |
| district | VARCHAR | 50 | NO | - | 区县 |
| address | VARCHAR | 255 | NO | - | 详细地址 |
| is_default | TINYINT | - | NO | 0 | 是否默认（0:否 1:是） |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT | 更新时间 |
| deleted_at | DATETIME | - | YES | NULL | 软删除时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_user_id` (`user_id`)
- KEY `idx_is_default` (`is_default`)

---

### 2.3 商品分类表（categories）

**功能**：商品分类管理

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | INT | - | NO | AUTO | 分类ID（主键） |
| parent_id | INT | - | NO | 0 | 父分类ID（0为顶级） |
| name | VARCHAR | 50 | NO | - | 分类名称 |
| icon | VARCHAR | 255 | YES | NULL | 分类图标 |
| sort | INT | - | NO | 0 | 排序（越大越靠前） |
| status | TINYINT | - | NO | 1 | 状态（0:禁用 1:启用） |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_parent_id` (`parent_id`)
- KEY `idx_sort` (`sort`)

---

### 2.4 商品表（products）

**功能**：存储商品信息

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 商品ID（主键） |
| category_id | INT | - | NO | - | 分类ID |
| name | VARCHAR | 100 | NO | - | 商品名称 |
| subtitle | VARCHAR | 255 | YES | NULL | 副标题 |
| cover | VARCHAR | 255 | NO | - | 封面图 |
| description | TEXT | - | YES | NULL | 商品描述 |
| price | DECIMAL | 10,2 | NO | 0.00 | 售价 |
| original_price | DECIMAL | 10,2 | YES | NULL | 原价 |
| cost_price | DECIMAL | 10,2 | YES | NULL | 成本价 |
| stock | INT | - | NO | 0 | 库存数量 |
| sales | INT | - | NO | 0 | 销量 |
| unit | VARCHAR | 20 | YES | '件' | 单位 |
| weight | DECIMAL | 10,2 | YES | NULL | 重量（kg） |
| tags | VARCHAR | 255 | YES | NULL | 标签（JSON数组） |
| is_hot | TINYINT | - | NO | 0 | 是否热门 |
| is_new | TINYINT | - | NO | 0 | 是否新品 |
| is_recommend | TINYINT | - | NO | 0 | 是否推荐 |
| sort | INT | - | NO | 0 | 排序 |
| status | TINYINT | - | NO | 1 | 状态（0:下架 1:上架） |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT | 更新时间 |
| deleted_at | DATETIME | - | YES | NULL | 软删除时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_category_id` (`category_id`)
- KEY `idx_status` (`status`)
- KEY `idx_is_hot` (`is_hot`)
- KEY `idx_is_new` (`is_new`)
- KEY `idx_sort` (`sort`)
- KEY `idx_created_at` (`created_at`)

---

### 2.5 商品图片表（product_images）

**功能**：存储商品详情图片

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 图片ID（主键） |
| product_id | BIGINT | - | NO | - | 商品ID |
| url | VARCHAR | 255 | NO | - | 图片URL |
| sort | INT | - | NO | 0 | 排序 |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_product_id` (`product_id`)

---

### 2.6 购物车表（cart）

**功能**：存储用户购物车数据

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 购物车ID（主键） |
| user_id | BIGINT | - | NO | - | 用户ID |
| product_id | BIGINT | - | NO | - | 商品ID |
| quantity | INT | - | NO | 1 | 数量 |
| selected | TINYINT | - | NO | 1 | 是否选中（0:否 1:是） |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `idx_user_product` (`user_id`, `product_id`)
- KEY `idx_user_id` (`user_id`)

---

### 2.7 订单表（orders）

**功能**：存储订单主表信息

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 订单ID（主键） |
| order_no | VARCHAR | 32 | NO | - | 订单号（唯一） |
| user_id | BIGINT | - | NO | - | 用户ID |
| total_amount | DECIMAL | 10,2 | NO | 0.00 | 商品总额 |
| freight_amount | DECIMAL | 10,2 | NO | 0.00 | 运费 |
| discount_amount | DECIMAL | 10,2 | NO | 0.00 | 优惠金额 |
| pay_amount | DECIMAL | 10,2 | NO | 0.00 | 实付金额 |
| pay_method | TINYINT | - | YES | NULL | 支付方式（1:微信 2:余额） |
| pay_time | DATETIME | - | YES | NULL | 支付时间 |
| delivery_type | TINYINT | - | NO | 1 | 配送方式（1:快递 2:自提） |
| consignee | VARCHAR | 50 | NO | - | 收货人 |
| phone | VARCHAR | 11 | NO | - | 收货电话 |
| province | VARCHAR | 50 | NO | - | 省份 |
| city | VARCHAR | 50 | NO | - | 城市 |
| district | VARCHAR | 50 | NO | - | 区县 |
| address | VARCHAR | 255 | NO | - | 详细地址 |
| status | TINYINT | - | NO | 0 | 订单状态 |
| remark | VARCHAR | 255 | YES | NULL | 备注 |
| coupon_id | BIGINT | - | YES | NULL | 使用的优惠券ID |
| points_used | INT | - | NO | 0 | 使用积分 |
| points_earned | INT | - | NO | 0 | 获得积分 |
| ship_time | DATETIME | - | YES | NULL | 发货时间 |
| ship_no | VARCHAR | 100 | YES | NULL | 物流单号 |
| ship_company | VARCHAR | 50 | YES | NULL | 物流公司 |
| finish_time | DATETIME | - | YES | NULL | 完成时间 |
| cancel_time | DATETIME | - | YES | NULL | 取消时间 |
| cancel_reason | VARCHAR | 255 | YES | NULL | 取消原因 |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT | 更新时间 |

**订单状态说明：**
- 0: 待支付
- 1: 待发货
- 2: 待收货
- 3: 已完成
- 4: 已取消
- 5: 退款中
- 6: 已退款

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `idx_order_no` (`order_no`)
- KEY `idx_user_id` (`user_id`)
- KEY `idx_status` (`status`)
- KEY `idx_created_at` (`created_at`)

---

### 2.8 订单商品表（order_items）

**功能**：存储订单商品详情

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 订单商品ID（主键） |
| order_id | BIGINT | - | NO | - | 订单ID |
| product_id | BIGINT | - | NO | - | 商品ID |
| product_name | VARCHAR | 100 | NO | - | 商品名称（快照） |
| product_cover | VARCHAR | 255 | NO | - | 商品封面（快照） |
| price | DECIMAL | 10,2 | NO | 0.00 | 商品单价 |
| quantity | INT | - | NO | 1 | 购买数量 |
| total_amount | DECIMAL | 10,2 | NO | 0.00 | 小计金额 |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_order_id` (`order_id`)
- KEY `idx_product_id` (`product_id`)

---

### 2.9 订单日志表（order_logs）

**功能**：记录订单状态变更日志

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 日志ID（主键） |
| order_id | BIGINT | - | NO | - | 订单ID |
| operator | VARCHAR | 50 | NO | - | 操作人 |
| content | VARCHAR | 255 | NO | - | 操作内容 |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_order_id` (`order_id`)

---

### 2.10 优惠券表（coupons）

**功能**：存储优惠券信息

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 优惠券ID（主键） |
| name | VARCHAR | 100 | NO | - | 优惠券名称 |
| type | TINYINT | - | NO | 1 | 类型（1:满减 2:折扣 3:无门槛） |
| discount_type | TINYINT | - | NO | 1 | 优惠类型（1:金额 2:折扣） |
| discount_value | DECIMAL | 10,2 | NO | 0.00 | 优惠值（金额或折扣） |
| min_amount | DECIMAL | 10,2 | NO | 0.00 | 最低消费金额 |
| total_count | INT | - | NO | 0 | 发放总数（0:不限） |
| receive_count | INT | - | NO | 0 | 已领取数量 |
| used_count | INT | - | NO | 0 | 已使用数量 |
| per_limit | INT | - | NO | 1 | 每人限领数量 |
| start_time | DATETIME | - | NO | - | 开始时间 |
| end_time | DATETIME | - | NO | - | 结束时间 |
| status | TINYINT | - | NO | 1 | 状态（0:禁用 1:启用） |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_status` (`status`)
- KEY `idx_time` (`start_time`, `end_time`)

---

### 2.11 用户优惠券表（user_coupons）

**功能**：存储用户领取的优惠券

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 记录ID（主键） |
| user_id | BIGINT | - | NO | - | 用户ID |
| coupon_id | BIGINT | - | NO | - | 优惠券ID |
| order_id | BIGINT | - | YES | NULL | 使用的订单ID |
| status | TINYINT | - | NO | 0 | 状态（0:未使用 1:已使用 2:已过期） |
| receive_time | DATETIME | - | NO | CURRENT | 领取时间 |
| use_time | DATETIME | - | YES | NULL | 使用时间 |
| expire_time | DATETIME | - | NO | - | 过期时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_user_id` (`user_id`)
- KEY `idx_coupon_id` (`coupon_id`)
- KEY `idx_status` (`status`)

---

### 2.12 积分记录表（points_logs）

**功能**：记录用户积分变动

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | BIGINT | - | NO | AUTO | 记录ID（主键） |
| user_id | BIGINT | - | NO | - | 用户ID |
| points | INT | - | NO | 0 | 积分变动（正数为增加，负数为减少） |
| type | TINYINT | - | NO | - | 类型（1:签到 2:消费 3:兑换 4:退款） |
| order_id | BIGINT | - | YES | NULL | 关联订单ID |
| remark | VARCHAR | 255 | YES | NULL | 备注 |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_user_id` (`user_id`)
- KEY `idx_type` (`type`)
- KEY `idx_created_at` (`created_at`)

---

### 2.13 轮播图表（banners）

**功能**：首页轮播图管理

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | INT | - | NO | AUTO | 轮播图ID（主键） |
| title | VARCHAR | 100 | NO | - | 标题 |
| image | VARCHAR | 255 | NO | - | 图片URL |
| link_type | TINYINT | - | NO | 1 | 链接类型（1:商品 2:分类 3:外链） |
| link_value | VARCHAR | 255 | YES | NULL | 链接值 |
| sort | INT | - | NO | 0 | 排序 |
| status | TINYINT | - | NO | 1 | 状态（0:禁用 1:启用） |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- KEY `idx_sort` (`sort`)
- KEY `idx_status` (`status`)

---

### 2.14 管理员表（admins）

**功能**：后台管理员账号

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | INT | - | NO | AUTO | 管理员ID（主键） |
| username | VARCHAR | 50 | NO | - | 用户名（唯一） |
| password | VARCHAR | 255 | NO | - | 密码（bcrypt加密） |
| nickname | VARCHAR | 50 | YES | NULL | 昵称 |
| avatar | VARCHAR | 255 | YES | NULL | 头像 |
| role | TINYINT | - | NO | 2 | 角色（1:超级管理员 2:普通管理员） |
| status | TINYINT | - | NO | 1 | 状态（0:禁用 1:启用） |
| last_login_at | DATETIME | - | YES | NULL | 最后登录时间 |
| last_login_ip | VARCHAR | 50 | YES | NULL | 最后登录IP |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `idx_username` (`username`)

---

### 2.15 系统配置表（configs）

**功能**：系统配置项

| 字段名 | 类型 | 长度 | 允许NULL | 默认值 | 说明 |
|--------|------|------|----------|--------|------|
| id | INT | - | NO | AUTO | 配置ID（主键） |
| key | VARCHAR | 100 | NO | - | 配置键（唯一） |
| value | TEXT | - | YES | NULL | 配置值 |
| description | VARCHAR | 255 | YES | NULL | 描述 |
| created_at | DATETIME | - | NO | CURRENT | 创建时间 |
| updated_at | DATETIME | - | NO | CURRENT | 更新时间 |

**索引：**
- PRIMARY KEY (`id`)
- UNIQUE KEY `idx_key` (`key`)

---

## 三、表关系图

```
users (用户表)
  ├── 1:N -> addresses (收货地址)
  ├── 1:N -> cart (购物车)
  ├── 1:N -> orders (订单)
  ├── 1:N -> user_coupons (用户优惠券)
  └── 1:N -> points_logs (积分记录)

categories (分类表)
  └── 1:N -> products (商品)

products (商品表)
  ├── 1:N -> product_images (商品图片)
  ├── 1:N -> cart (购物车)
  └── 1:N -> order_items (订单商品)

orders (订单表)
  ├── 1:N -> order_items (订单商品)
  └── 1:N -> order_logs (订单日志)

coupons (优惠券表)
  └── 1:N -> user_coupons (用户优惠券)
```

---

## 四、初始化数据

### 4.1 默认管理员
```sql
-- 用户名: admin
-- 密码: admin123 (需要 bcrypt 加密后插入)
```

### 4.2 默认分类
- 膨化食品
- 糖果巧克力
- 肉类零食
- 坚果炒货
- 果干蜜饯
- 饼干糕点

### 4.3 系统配置
- 运费规则
- 积分规则
- 会员等级规则
- 小程序配置

---

**文档版本**：v1.0  
**创建时间**：2026-08-12

