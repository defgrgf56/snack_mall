# 首页功能扩展部署指南

本指南将帮助你完成首页三大功能模块的部署：限时秒杀、优惠券领取中心、活动专区。

## 📋 功能概述

### 1. 限时秒杀模块 ⚡
- 实时倒计时显示
- 秒杀价格和折扣展示
- 商品抢购进度条
- 点击跳转商品详情

### 2. 优惠券领取中心 🎫
- 横向滚动卡片展示
- 满减券/折扣券类型
- 一键领取功能
- 我的优惠券入口

### 3. 活动专区 🎉
- 网格布局展示
- 四种活动类型（节日促销、新人专享、会员专区、拼团活动）
- 精美图片展示
- 活动标签和倒计时

## 🚀 快速部署（3步）

### 第一步：初始化数据库表

```bash
cd 后端API\migrations
# 运行批处理文件
init-seckill-activity.bat
```

**手动执行（可选）：**
```bash
mysql -h localhost -u root -p snack_mall < create-seckill-activity-tables.sql
```

### 第二步：导入测试数据

```bash
cd 后端API\migrations
# 运行批处理文件
导入秒杀活动测试数据.bat
```

**手动执行（可选）：**
```bash
mysql -h localhost -u root -p snack_mall < test-data-seckill-activity.sql
```

### 第三步：重启后端服务

```bash
cd 后端API
# 停止当前服务（Ctrl+C）
# 重新启动
npm run dev
```

## ✅ 验证部署

### 1. 检查数据库表

```sql
-- 检查表是否创建成功
SHOW TABLES LIKE '%seckill%';
SHOW TABLES LIKE '%activit%';

-- 查看数据
SELECT COUNT(*) FROM seckills;
SELECT COUNT(*) FROM activities;
SELECT COUNT(*) FROM coupons;
```

### 2. 测试后端API

```bash
# 测试秒杀接口
curl http://localhost:3000/api/seckills

# 测试活动接口
curl http://localhost:3000/api/activities

# 测试优惠券接口
curl http://localhost:3000/api/coupons/available
```

### 3. 测试小程序前端

1. 打开微信开发者工具
2. 编译项目
3. 查看首页是否显示三个新模块
4. 测试功能：
   - ✅ 秒杀倒计时是否正常运行
   - ✅ 优惠券是否能正常领取
   - ✅ 活动卡片是否正常显示

## 📊 数据库表结构

### seckills（秒杀活动表）
```sql
- id: 主键
- product_id: 商品ID
- title: 秒杀标题
- seckill_price: 秒杀价格
- original_price: 原价
- stock: 库存
- sold: 已售数量
- start_time: 开始时间
- end_time: 结束时间
- status: 状态（0-已结束 1-进行中 2-未开始）
```

### activities（活动专区表）
```sql
- id: 主键
- title: 活动标题
- subtitle: 副标题
- cover: 封面图
- type: 类型（festival/newbie/vip/group）
- start_time: 开始时间
- end_time: 结束时间
- status: 状态
```

### activity_products（活动商品关联表）
```sql
- id: 主键
- activity_id: 活动ID
- product_id: 商品ID
- discount: 折扣
- special_price: 特价
```

## 🎨 前端文件修改清单

### 小程序前端修改
- ✅ `pages/index/index.wxml` - 添加三个新模块的UI
- ✅ `pages/index/index.wxss` - 添加模块样式
- ✅ `pages/index/index.js` - 添加数据加载和交互逻辑
- ✅ `config/api.js` - 添加优惠券API接口

### 后端API修改
- ✅ `src/models/Seckill.js` - 秒杀模型
- ✅ `src/models/Activity.js` - 活动模型
- ✅ `src/models/ActivityProduct.js` - 活动商品关联模型
- ✅ `src/routes/seckill.js` - 秒杀路由
- ✅ `src/routes/activity.js` - 活动路由
- ✅ `src/routes/index.js` - 路由注册

## 🔧 自定义配置

### 修改测试数据

编辑 `test-data-seckill-activity.sql`：

```sql
-- 修改秒杀商品
INSERT INTO seckills (product_id, title, seckill_price, ...) VALUES
(你的商品ID, '你的标题', 你的价格, ...);

-- 修改活动信息
INSERT INTO activities (title, cover, type, ...) VALUES
('你的活动', '你的图片URL', '活动类型', ...);
```

### 修改倒计时时长

在 `index.js` 的 `startSeckillCountdown()` 方法中调整。

### 修改显示数量

在 `index.js` 的 `loadData()` 方法中：
```javascript
coupons: coupons.slice(0, 5),    // 优惠券数量
seckills: seckills.slice(0, 10), // 秒杀商品数量
activities: activities.slice(0, 4) // 活动数量
```

## 📱 功能特性

### 秒杀模块特性
- ✅ 自动倒计时，精确到秒
- ✅ 动态计算折扣百分比
- ✅ 实时显示抢购进度
- ✅ 倒计时结束自动刷新
- ✅ 响应式布局适配

### 优惠券模块特性
- ✅ 满减券/折扣券展示
- ✅ 登录检测（未登录跳转）
- ✅ 领取限制提示
- ✅ 领取成功反馈
- ✅ 跳转我的优惠券

### 活动模块特性
- ✅ 四种活动类型标签
- ✅ 图片遮罩渐变效果
- ✅ 活动倒计时（后续扩展）
- ✅ 网格布局自适应
- ✅ 点击跳转活动详情

## 🐛 常见问题

### 问题1：秒杀数据不显示

**原因：** 
- 商品表中没有对应的商品数据
- 秒杀时间已过期

**解决：**
```sql
-- 检查商品是否存在
SELECT * FROM products WHERE id IN (1,2,3,4,5);

-- 更新秒杀时间
UPDATE seckills SET 
  start_time = NOW(), 
  end_time = DATE_ADD(NOW(), INTERVAL 2 HOUR)
WHERE status = 1;
```

### 问题2：优惠券领取失败

**原因：** 
- 未登录
- 已达到领取上限
- 优惠券已领完

**解决：**
- 确保已登录
- 检查优惠券配置
- 增加优惠券库存

### 问题3：活动图片不显示

**原因：** 
- 图片URL无效
- 网络问题

**解决：**
```sql
-- 更新活动封面图
UPDATE activities SET 
  cover = 'https://你的图片URL.jpg'
WHERE id = 1;
```

### 问题4：倒计时不更新

**原因：** 
- 定时器未启动
- 页面已卸载

**解决：**
- 检查 `startSeckillCountdown()` 是否被调用
- 确保 `onUnload()` 中清除了定时器

## 📈 后续扩展建议

### 短期扩展
- [ ] 秒杀列表页（显示所有秒杀商品）
- [ ] 活动详情页（展示活动商品列表）
- [ ] 优惠券详情页
- [ ] 秒杀提醒功能

### 中期扩展
- [ ] 秒杀抢购功能（下单限制）
- [ ] 拼团功能实现
- [ ] 活动分享功能
- [ ] 秒杀历史记录

### 长期扩展
- [ ] 积分兑换商城
- [ ] 会员等级体系
- [ ] 签到领券功能
- [ ] 推荐算法（猜你喜欢）

## 📝 API接口文档

### 秒杀接口

**获取秒杀列表**
```
GET /api/seckills
参数：status=1（进行中）, page=1, pageSize=10
返回：秒杀列表 + 剩余时间 + 进度
```

**获取秒杀详情**
```
GET /api/seckills/:id
返回：秒杀详情 + 商品信息
```

### 活动接口

**获取活动列表**
```
GET /api/activities
参数：type（可选）, status=1, page=1, pageSize=10
返回：活动列表
```

**获取活动详情**
```
GET /api/activities/:id
返回：活动详情 + 关联商品
```

**获取活动商品**
```
GET /api/activities/:id/products
返回：活动商品列表 + 活动价格
```

### 优惠券接口

**获取可用优惠券**
```
GET /api/coupons/available
返回：可领取的优惠券列表
```

**领取优惠券**
```
POST /api/coupons/receive
Body: { coupon_id: 1 }
需要登录
```

**我的优惠券**
```
GET /api/coupons/my?status=0
参数：status（0-未使用 1-已使用 2-已过期）
需要登录
```

## ✨ 效果展示

### 首页效果
```
┌─────────────────────────────────┐
│  🏠 阿宇商城                     │
├─────────────────────────────────┤
│  🔍 搜索零食                     │
├─────────────────────────────────┤
│  [轮播图]                        │
├─────────────────────────────────┤
│  🎫 优惠券中心    我的优惠券 >   │
│  [满减券] [折扣券] [新人券] ... │
├─────────────────────────────────┤
│  ⚡ 限时秒杀  距结束 01:25:38   │
│  [商品1] [商品2] [商品3] ...    │
├─────────────────────────────────┤
│  🎉 活动专区                     │
│  [节日促销] [新人专享]          │
│  [会员专区] [拼团活动]          │
├─────────────────────────────────┤
│  🔥 热门推荐                     │
│  ...                             │
└─────────────────────────────────┘
```

## 🎉 部署完成

恭喜！如果以上步骤都完成了，你的小程序商城首页现在应该有了三个全新的功能模块。

**测试清单：**
- ✅ 秒杀倒计时正常运行
- ✅ 优惠券可以正常领取
- ✅ 活动卡片显示正常
- ✅ 所有跳转功能正常
- ✅ 响应式布局适配良好

**下一步：**
1. 根据实际需求调整测试数据
2. 上传真实的商品图片和活动封面
3. 设置合适的秒杀时间和活动周期
4. 配置合理的优惠券规则

---

**开发团队：** Kiro AI  
**文档版本：** v1.0  
**更新日期：** 2026-08-20
