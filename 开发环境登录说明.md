# 🔧 开发环境登录说明

## ⚠️ 游客模式问题

在微信开发者工具的**游客模式**（没有真实AppID）下，某些API会受限：

```
wx.getUserProfile:fail getUserAvatarInfo fail
```

---

## ✅ 解决方案

### 方案1: 使用真实AppID（推荐）

1. **注册微信小程序**
   - 访问: https://mp.weixin.qq.com/
   - 注册并获取AppID

2. **配置AppID**
   ```json
   // 小程序前端/project.config.json
   {
     "appid": "wx1234567890abcdef"  // 你的真实AppID
   }
   ```

3. **配置后端**
   ```env
   # 后端API/.env
   WX_APPID=wx1234567890abcdef
   WX_SECRET=你的AppSecret
   ```

4. **重启微信开发者工具**

### 方案2: 开发模式简化登录（当前已实现）

如果暂时没有AppID，项目已经支持**开发模式自动登录**：

#### 前端逻辑
```javascript
// pages/user/user.js
async onLogin() {
  // 检测游客模式
  const accountInfo = wx.getAccountInfoSync()
  
  if (游客模式) {
    // 使用简化登录流程
    await app.login()  // 不需要getUserProfile
    
    // 使用默认用户信息
    app.globalData.userInfo = {
      nickname: '测试用户',
      avatar: 'https://img.yzcdn.cn/vant/cat.jpeg'
    }
  } else {
    // 真实环境：使用getUserProfile
    const profile = await wx.getUserProfile({...})
  }
}
```

#### 后端逻辑
```javascript
// routes/auth.js
router.post('/login', async (req, res) => {
  // 检测是否配置了微信AppID
  if (!process.env.WX_APPID || process.env.WX_APPID === 'your_appid_here') {
    // 开发模式：创建测试用户
    let user = await User.findOne({ where: { phone: '13800138000' } })
    if (!user) {
      user = await User.create({
        openid: 'test_openid_' + Date.now(),
        nickname: '测试用户',
        phone: '13800138000'
      })
    }
    
    // 生成token
    const token = jwt.sign(...)
    
    return res.json({
      code: 200,
      message: '登录成功（开发模式）',
      data: { token, userInfo }
    })
  }
  
  // 生产模式：调用微信API
  // ...
})
```

---

## 🧪 测试开发模式登录

### 步骤1: 确保后端未配置微信AppID

编辑 `后端API/.env`:
```env
# 留空或使用占位符
WX_APPID=your_appid_here
WX_SECRET=your_app_secret_here

# 或直接注释掉
# WX_APPID=
# WX_SECRET=
```

### 步骤2: 重启后端

```bash
cd 后端API
npm run dev
```

查看Console输出:
```
⚠️  开发模式：使用测试账号登录
```

### 步骤3: 前端登录测试

1. 打开小程序"我的"页面
2. 点击"立即登录"
3. 看到提示: "当前为开发模式，将使用测试账号登录"
4. 点击"确定"
5. 登录成功 ✅

**预期结果**:
- 显示用户昵称: "测试用户"
- 显示默认头像
- Token已保存
- 可以访问需要登录的接口

---

## 🔍 判断当前模式

### 在前端判断

```javascript
// 获取账号信息
const accountInfo = wx.getAccountInfoSync()

console.log('AppID:', accountInfo.miniProgram.appId)
console.log('环境:', accountInfo.miniProgram.envVersion)
// develop: 开发版
// trial: 体验版  
// release: 正式版

// 判断是否为游客模式
const isGuest = !accountInfo.miniProgram.appId || 
                accountInfo.miniProgram.appId === ''
```

### 在后端判断

```javascript
// 检查环境变量
const isDev = !process.env.WX_APPID || 
              process.env.WX_APPID === 'your_appid_here'

if (isDev) {
  console.log('⚠️  开发模式')
} else {
  console.log('✅ 生产模式')
}
```

---

## 📊 两种模式对比

| 功能 | 开发模式 | 生产模式 |
|------|----------|----------|
| 需要AppID | ❌ 否 | ✅ 是 |
| getUserProfile | ❌ 跳过 | ✅ 需要 |
| 用户授权 | ❌ 不需要 | ✅ 需要 |
| 用户信息 | 🔧 模拟数据 | ✅ 真实数据 |
| openid | 🔧 测试openid | ✅ 真实openid |
| 购物车功能 | ✅ 可用 | ✅ 可用 |
| 订单功能 | ✅ 可用 | ✅ 可用 |
| 支付功能 | ❌ 不可用 | ✅ 可用 |

---

## 🎯 开发建议

### 开发阶段（无AppID）

✅ 使用开发模式:
- 快速测试功能
- 不需要真实微信账号
- 专注业务逻辑开发

### 测试阶段（有AppID）

✅ 配置真实AppID:
- 测试完整登录流程
- 验证用户授权
- 测试真实环境

### 上线前（必须有AppID）

✅ 完整配置:
- 真实AppID和AppSecret
- HTTPS服务器域名
- 支付配置
- 完整测试

---

## ⚙️ 快速切换模式

### 切换到开发模式

1. **后端**: 编辑 `.env`
   ```env
   WX_APPID=your_appid_here
   WX_SECRET=your_app_secret_here
   ```

2. **重启后端**
   ```bash
   npm run dev
   ```

3. **前端**: 无需修改（自动检测）

### 切换到生产模式

1. **后端**: 编辑 `.env`
   ```env
   WX_APPID=wx1234567890abcdef
   WX_SECRET=abc123def456...
   ```

2. **前端**: 编辑 `project.config.json`
   ```json
   {
     "appid": "wx1234567890abcdef"
   }
   ```

3. **重启服务**
   - 后端: `npm run dev`
   - 前端: 微信开发者工具重新编译

---

## 🐛 常见问题

### 问题1: 一直显示"登录失败"

**检查**:
```bash
# 查看后端日志
# 应该显示: ⚠️  开发模式：使用测试账号登录
```

**解决**:
1. 确认后端 `.env` 中 `WX_APPID` 为空或占位符
2. 重启后端服务
3. 清除小程序Storage: Console执行 `wx.clearStorageSync()`

### 问题2: 显示"Token无效"

**原因**: JWT_SECRET未配置

**解决**:
```env
# .env
JWT_SECRET=any_random_string_at_least_32_characters_long
```

### 问题3: 前端一直转圈

**检查**:
```javascript
// Console查看
console.log(wx.getStorageSync('token'))
```

**解决**:
1. 检查网络请求（Network面板）
2. 查看后端是否启动
3. 检查apiBase配置

---

## 📝 测试清单

### 开发模式测试
- [ ] 点击登录按钮
- [ ] 看到"开发模式"提示
- [ ] 点击确定
- [ ] 显示"测试用户"
- [ ] Token已保存
- [ ] 可以访问需要登录的接口

### 生产模式测试
- [ ] 配置真实AppID
- [ ] 点击登录按钮
- [ ] 弹出授权窗口
- [ ] 授权后登录成功
- [ ] 显示真实用户信息
- [ ] Token已保存

---

## 🔄 登录流程对比

### 开发模式流程
```
点击登录
    ↓
检测到游客模式
    ↓
wx.login() 获取code
    ↓
POST /api/auth/login (code)
    ↓
后端检测到开发模式
    ↓
创建/查找测试用户
    ↓
生成JWT token
    ↓
返回 token + 默认用户信息
    ↓
前端保存token
    ↓
使用默认头像昵称
    ↓
登录完成 ✅
```

### 生产模式流程
```
点击登录
    ↓
wx.getUserProfile() 获取授权
    ↓
wx.login() 获取code
    ↓
POST /api/auth/login (code)
    ↓
后端调用微信API
    ↓
获取真实openid
    ↓
创建/查找真实用户
    ↓
生成JWT token
    ↓
返回 token + 真实用户信息
    ↓
前端保存token
    ↓
更新用户资料
    ↓
登录完成 ✅
```

---

## ✅ 总结

### 当前实现特点

1. **自动检测环境**
   - 无需手动切换代码
   - 根据AppID自动判断

2. **开发友好**
   - 无AppID也能测试
   - 快速开发调试

3. **生产就绪**
   - 配置AppID即可上线
   - 完整登录流程

4. **平滑过渡**
   - 同一套代码
   - 无需改动逻辑

---

**建议**: 
- 开发阶段使用开发模式快速迭代
- 测试阶段配置真实AppID验证流程
- 上线前完成所有配置和测试

---

**创建时间**: 2026年8月14日  
**适用场景**: 开发环境、游客模式  
**版本**: v1.0
