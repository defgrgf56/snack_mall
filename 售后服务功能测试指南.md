# 售后服务(退换货)功能测试指南

## 📋 功能概述

已完成的售后服务功能包括:
- ✅ 申请退款(仅退款/退货退款)
- ✅ 退款详情查看
- ✅ 退款列表(状态筛选)
- ✅ 取消退款申请
- ✅ 退款进度跟踪
- ✅ 管理员审核(API)

---

## 🗄️ 数据库初始化

### 1. 创建退款表

**方法一: 使用批处理文件**
```bash
cd 后端API/migrations
init-refund-tables.bat
```

**方法二: 手动执行SQL**
```bash
mysql -u root -p666666 snack_mall < 后端API/migrations/create-refunds-table.sql
```

### 2. 验证表创建

```sql
USE snack_mall;

-- 查看退款表
DESCRIBE refunds;

-- 查看退款日志表
DESCRIBE refund_logs;
```

---

## 🚀 启动服务

### 1. 启动后端API

```bash
cd 后端API
npm run dev
```

**验证启动成功:**
- 访问: `http://localhost:3000/api`
- 应返回: `{ "code": 200, "message": "API is running" }`

### 2. 启动小程序

1. 打开微信开发者工具
2. 导入项目: `小程序前端`
3. AppID: `wxdca249674206beb2`
4. 编译运行

---

## 🧪 完整测试流程

### 测试前提条件

1. **已登录用户**
   - 开发环境自动使用测试账号
   - 或使用微信授权登录

2. **已有订单数据**
   - 至少有一个已完成的订单(status=4)
   - 或已发货的订单(status=3)

---

## 📱 前端测试流程

### 测试一: 申请退款

**步骤:**

1. **进入订单详情**
   - 路径: 我的 → 订单列表 → 选择已完成订单
   - 或: 我的 → 待收货/已完成 → 订单详情

2. **点击"申请退款"按钮**
   - 只有状态为2(待发货)、3(待收货)、4(已完成)的订单显示此按钮

3. **填写退款信息**
   - 选择退款类型: 
     - ✅ 仅退款(未收货/协商)
     - ✅ 退货退款(已收货)
   - 选择退款原因: 必填项
   - 填写退款说明: 选填
   - 上传凭证图片: 最多3张,选填

4. **提交申请**
   - 点击"提交申请"
   - 二次确认弹窗
   - 提交成功后自动跳转到退款详情页

**预期结果:**
- ✅ 退款申请提交成功
- ✅ 自动跳转到退款详情页
- ✅ 订单状态更新为"退款中"(status=5)
- ✅ 退款状态为"待审核"(status=0)

---

### 测试二: 查看退款详情

**步骤:**

1. **进入方式**
   - 申请成功后自动跳转
   - 或: 我的 → 退款/售后 → 选择退款记录

2. **查看退款信息**
   - ✅ 退款单号
   - ✅ 退款类型
   - ✅ 退款原因
   - ✅ 退款金额
   - ✅ 退款说明
   - ✅ 退款凭证图片
   - ✅ 申请时间

3. **查看订单信息**
   - ✅ 订单号
   - ✅ 商品列表
   - ✅ 商品图片、名称、价格、数量

4. **查看退款进度**
   - ✅ 时间轴展示
   - ✅ 每个状态的时间和操作人
   - ✅ 最新状态在最上方

5. **操作按钮(状态=待审核时)**
   - ✅ 显示"取消申请"按钮

**预期结果:**
- ✅ 所有信息正确显示
- ✅ 图片可点击预览
- ✅ 退款进度清晰展示

---

### 测试三: 取消退款

**步骤:**

1. **在退款详情页点击"取消申请"**
2. **确认弹窗**
3. **取消成功**

**预期结果:**
- ✅ 退款状态更新为"已取消"(status=5)
- ✅ 订单状态恢复为"已完成"(status=3)
- ✅ 退款进度新增取消记录
- ✅ "取消申请"按钮消失

---

### 测试四: 退款列表

**步骤:**

1. **进入退款列表**
   - 路径: 我的 → 退款/售后

2. **查看列表数据**
   - ✅ 显示所有退款记录
   - ✅ 退款单号
   - ✅ 商品信息(图片、名称)
   - ✅ 退款类型、原因
   - ✅ 退款金额
   - ✅ 申请时间
   - ✅ 退款状态(不同颜色标识)

3. **状态筛选**
   - ✅ 全部
   - ✅ 待审核
   - ✅ 退款中
   - ✅ 已完成
   - ✅ 已拒绝

4. **下拉刷新**
   - 下拉页面刷新数据

5. **上拉加载**
   - 滚动到底部加载更多

6. **点击记录**
   - 跳转到退款详情页

**预期结果:**
- ✅ 列表数据正确显示
- ✅ 状态筛选正常工作
- ✅ 刷新和加载功能正常
- ✅ 跳转详情页正常

---

## 🔧 API测试

### 测试工具
- Postman
- 或浏览器扩展(如Talend API Tester)

### 获取Token

**登录接口:**
```
POST http://localhost:3000/api/auth/login
Content-Type: application/json

{
  "code": "test_code"
}
```

**返回:**
```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "userInfo": { ... }
  }
}
```

**使用Token:**
在后续请求的Header中添加:
```
Authorization: Bearer <token>
```

---

### API测试用例

#### 1. 申请退款

**请求:**
```
POST http://localhost:3000/api/refunds
Authorization: Bearer <token>
Content-Type: application/json

{
  "order_id": 1,
  "refund_type": 1,
  "refund_reason": "不想要了",
  "refund_desc": "商品不合适",
  "refund_images": []
}
```

**预期响应:**
```json
{
  "code": 200,
  "message": "退款申请已提交",
  "data": {
    "id": 1,
    "refund_no": "R20260817XXXX"
  }
}
```

---

#### 2. 获取退款列表

**请求:**
```
GET http://localhost:3000/api/refunds?page=1&pageSize=10
Authorization: Bearer <token>
```

**预期响应:**
```json
{
  "code": 200,
  "data": {
    "list": [
      {
        "id": 1,
        "refund_no": "R20260817XXXX",
        "refund_type": 1,
        "refund_type_text": "仅退款",
        "refund_reason": "不想要了",
        "refund_amount": "25.60",
        "status": 0,
        "status_text": "待审核",
        "created_at": "2026-08-17 10:00:00"
      }
    ],
    "pagination": {
      "total": 1,
      "page": 1,
      "pageSize": 10,
      "totalPages": 1
    }
  }
}
```

---

#### 3. 获取退款详情

**请求:**
```
GET http://localhost:3000/api/refunds/1
Authorization: Bearer <token>
```

**预期响应:**
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "refund_no": "R20260817XXXX",
    "refund_type": 1,
    "refund_type_text": "仅退款",
    "refund_reason": "不想要了",
    "refund_amount": "25.60",
    "status": 0,
    "status_text": "待审核",
    "order": { ... },
    "logs": [ ... ]
  }
}
```

---

#### 4. 取消退款

**请求:**
```
PUT http://localhost:3000/api/refunds/1/cancel
Authorization: Bearer <token>
```

**预期响应:**
```json
{
  "code": 200,
  "message": "退款申请已取消"
}
```

---

#### 5. 管理员审核(需要管理员权限)

**请求:**
```
PUT http://localhost:3000/api/refunds/1/review
Authorization: Bearer <admin_token>
Content-Type: application/json

{
  "status": 1,
  "admin_remark": "审核通过"
}
```

**或拒绝:**
```json
{
  "status": 2,
  "reject_reason": "商品无质量问题",
  "admin_remark": "请联系客服"
}
```

**预期响应:**
```json
{
  "code": 200,
  "message": "审核通过"
}
```

---

## ✅ 测试检查清单

### 前端功能

- [ ] 申请退款页面
  - [ ] 选择退款类型
  - [ ] 选择退款原因
  - [ ] 填写退款说明
  - [ ] 上传图片(最多3张)
  - [ ] 提交成功跳转

- [ ] 退款详情页面
  - [ ] 退款状态显示
  - [ ] 退款信息完整
  - [ ] 订单信息显示
  - [ ] 退款进度时间轴
  - [ ] 取消退款功能

- [ ] 退款列表页面
  - [ ] 列表数据显示
  - [ ] 状态标签颜色
  - [ ] Tab切换筛选
  - [ ] 下拉刷新
  - [ ] 上拉加载
  - [ ] 点击跳转详情

- [ ] 入口显示
  - [ ] 订单详情页退款按钮
  - [ ] 用户中心退款入口

### 后端API

- [ ] POST /api/refunds - 申请退款
- [ ] GET /api/refunds - 退款列表
- [ ] GET /api/refunds/:id - 退款详情
- [ ] PUT /api/refunds/:id/cancel - 取消退款
- [ ] PUT /api/refunds/:id/review - 管理员审核

### 数据库

- [ ] refunds表数据正确插入
- [ ] refund_logs表记录状态变更
- [ ] 订单状态正确更新
- [ ] 退款单号生成唯一

### 状态流转

- [ ] 0(待审核) → 1(审核通过) → 3(退款中) → 4(退款成功)
- [ ] 0(待审核) → 2(审核拒绝)
- [ ] 0(待审核) → 5(已取消)

---

## 🐛 常见问题

### 1. 数据库连接失败

**问题:** `Error: connect ECONNREFUSED`

**解决:**
```bash
# 检查MySQL服务
net start MySQL

# 检查数据库配置
# 文件: 后端API/.env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=snack_mall
DB_USER=root
DB_PASSWORD=666666
```

### 2. Token无效

**问题:** `401 Unauthorized`

**解决:**
- 重新登录获取新token
- 检查token是否过期(默认7天)
- 确认Header格式: `Authorization: Bearer <token>`

### 3. 退款按钮不显示

**问题:** 订单详情页看不到"申请退款"按钮

**解决:**
- 检查订单状态(只有status=2/3/4显示)
- 待付款(status=1)不能申请退款
- 已取消(status=5)不能申请退款

### 4. 图片上传失败

**问题:** 上传凭证图片失败

**解决:**
- 检查uploads目录权限
- 检查图片大小(建议<2MB)
- 检查图片格式(支持JPG/PNG)

---

## 📊 测试数据示例

### 创建测试订单

```sql
-- 插入一个已完成的测试订单
INSERT INTO orders (user_id, order_no, total_amount, pay_amount, status, created_at) 
VALUES (1, 'TEST202608170001', 100.00, 100.00, 4, NOW());

-- 获取订单ID
SELECT LAST_INSERT_ID();

-- 插入订单商品
INSERT INTO order_items (order_id, product_id, product_name, product_cover, price, quantity) 
VALUES (LAST_INSERT_ID(), 1, '薯片（原味）', 'http://...', 12.80, 2);
```

---

## 📝 测试报告模板

```markdown
# 售后服务功能测试报告

**测试人员:** [姓名]
**测试日期:** 2026-08-17
**测试环境:** 开发环境

## 测试结果

### 前端功能
- [x] 申请退款: ✅ 通过
- [x] 退款详情: ✅ 通过
- [x] 退款列表: ✅ 通过
- [x] 取消退款: ✅ 通过

### 后端API
- [x] 申请退款API: ✅ 通过
- [x] 退款列表API: ✅ 通过
- [x] 退款详情API: ✅ 通过
- [x] 取消退款API: ✅ 通过
- [x] 管理员审核API: ✅ 通过

## 发现的问题
1. [问题描述]
   - 复现步骤: ...
   - 预期结果: ...
   - 实际结果: ...

## 总结
功能基本完整,可以投入使用。
```

---

## 🎉 测试完成

完成以上所有测试后,售后服务功能即可正式使用!

**功能特点:**
- ✅ 完整的退款流程
- ✅ 用户友好的界面
- ✅ 实时的进度跟踪
- ✅ 灵活的状态管理
- ✅ 完善的日志记录

**下一步:**
- 集成真实的退款支付接口
- 添加消息通知功能
- 完善管理后台审核界面
- 添加退款原因统计分析
