# 🔐 登录功能配置指南

## 📋 目录
1. [前置准备](#前置准备)
2. [微信公众平台配置](#微信公众平台配置)
3. [后端配置](#后端配置)
4. [前端配置](#前端配置)
5. [测试验证](#测试验证)
6. [常见问题](#常见问题)

---

## 1. 前置准备

### 需要的账号和工具
- ✅ 微信小程序账号（已注册并通过认证）
- ✅ 微信开发者工具
- ✅ Node.js 环境（后端）
- ✅ MySQL 数据库

### 检查清单
- [ ] 微信小程序已注册
- [ ] 获取了 AppID 和 AppSecret
- [ ] 后端API服务已启动
- [ ] 数据库已创建并初始化

---

## 2. 微信公众平台配置

### 步骤1: 获取 AppID 和 AppSecret

1. **登录微信公众平台**
   - 访问: https://mp.weixin.qq.com/
   - 使用微信扫码登录

2. **进入开发设置**
   ```
   左侧菜单: 开发 -> 开发管理 -> 开发设置
   ```

3. **复制配置信息**
   - **AppID(小程序ID)**: 形如 `wx1234567890abcdef`
   - **AppSecret(小程序密钥)**: 点击"生成"按钮获取
   
   ⚠️ **重要提示**:
   - AppSecret只显示一次，请妥善保存
   - 不要将AppSecret提交到Git或公开
   - 重置AppSecret会导致旧token失效

### 步骤2: 配置服务器域名（可选，开发环境可跳过）

在"开发设置"中配置:
- **request合法域名**: `https://your-domain.com`
- **uploadFile合法域名**: `https://your-domain.com`
- **downloadFile合法域名**: `https://your-domain.com`

⚠️ **开发环境**:
- 可以在微信开发者工具中勾选"不校验合法域名"
- 生产环境必须配置HTTPS域名

---

## 3. 后端配置

### 步骤1: 配置环境变量

1. **复制环境变量模板**
   ```bash
   cd 后端API
   copy .env.example .env
   ```

2. **编辑 `.env` 文件**
   ```env
   # 服务器配置
   PORT=3000
   NODE_ENV=development

   # 数据库配置
   DB_HOST=localhost
   DB_PORT=3306
   DB_NAME=snack_mall
   DB_USER=root
   DB_PASSWORD=666666

   # 微信小程序配置（重要！）
   WX_APPID=wx1234567890abcdef        # 替换为你的AppID
   WX_SECRET=your_app_secret_here      # 替换为你的AppSecret

   # JWT配置
   JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long
   JWT_EXPIRES_IN=7d

   # CORS配置
   CORS_ORIGIN=*
   ```

3. **生成 JWT_SECRET**（推荐）
   ```bash
   # Windows PowerShell
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   
   # 输出示例（复制到.env）:
   # 5f8e7d9c6b5a4e3d2c1b0a9f8e7d6c5b4a3e2d1c0b9a8f7e6d5c4b3a2e1d0c
   ```

### 步骤2: 安装依赖（如果还没有）

```bash
cd 后端API
npm install jsonwebtoken axios
```

### 步骤3: 注册路由

编辑 `后端API/src/app.js`:

```javascript
// 导入路由
const authRoutes = require('./routes/auth');
const userRoutes = require('./routes/user');

// 注册路由
app.use('/api/auth', authRoutes);
app.use('/api/user', userRoutes);

// ... 其他路由
```

### 步骤4: 重启后端服务

```bash
# 停止现有服务（Ctrl+C）
# 重新启动
npm run dev
```

**验证**:
- Console应该显示: `Server is running on port 3000`
- 没有报错信息

---

## 4. 前端配置

### 步骤1: 配置AppID

编辑 `小程序前端/project.config.json`:

```json
{
  "appid": "wx1234567890abcdef",  // 替换为你的AppID
  "projectname": "零食商城",
  // ... 其他配置
}
```

### 步骤2: 配置API地址

编辑 `小程序前端/app.js`:

```javascript
App({
  globalData: {
    apiBase: 'http://localhost:3000/api',  // 本地开发
    // apiBase: 'https://your-domain.com/api',  // 生产环境
    // ...
  }
})
```

### 步骤3: 开发者工具配置

在微信开发者工具中:

1. **打开项目设置**
   ```
   工具栏: 详情 -> 本地设置
   ```

2. **勾选以下选项**
   - ✅ 不校验合法域名、web-view（业务域名）、TLS版本以及HTTPS证书
   - ✅ 启用调试
   - ✅ 不校验证书

### 步骤4: 重新编译

点击微信开发者工具的"编译"按钮

---

## 5. 测试验证

### 测试步骤

#### 1. 测试后端接口

在PowerShell中测试:

```bash
# 测试登录接口（需要真实的微信code）
curl -X POST http://localhost:3000/api/auth/login `
  -H "Content-Type: application/json" `
  -d '{\"code\": \"test_code\"}'

# 预期响应（会失败，因为code无效，但说明接口通了）:
# {"code": 500, "message": "微信登录失败: invalid code"}
```

#### 2. 测试前端登录流程

1. **打开小程序"我的"页面**
   - 底部TabBar点击"我的"

2. **查看登录状态**
   - 未登录: 显示"未登录"和"立即登录"按钮
   - 已登录: 显示用户昵称和头像

3. **点击"立即登录"按钮**
   - 会弹出授权窗口
   - 点击"允许"授权

4. **查看Console日志**
   ```
   微信登录获取code -> 
   发送code到后端 ->
   后端调用微信API ->
   返回token ->
   保存token ->
   显示登录成功
   ```

5. **验证登录成功**
   - 页面显示用户昵称
   - 显示订单统计数据
   - 购物车徽标更新

#### 3. 测试Token验证

在微信开发者工具Console执行:

```javascript
// 查看token
const token = wx.getStorageSync('token')
console.log('当前token:', token)

// 测试获取用户信息
wx.request({
  url: 'http://localhost:3000/api/user/info',
  header: {
    'Authorization': `Bearer ${token}`
  },
  success: (res) => {
    console.log('用户信息:', res.data)
  }
})
```

**预期结果**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "nickname": "用户123456",
    "avatar": null,
    "phone": null,
    "level": 1,
    "points": 0
  }
}
```

#### 4. 测试退出登录

1. 点击"退出"按钮
2. 确认退出
3. 验证:
   - token已清除
   - 页面显示"未登录"
   - 购物车徽标清除

---

## 6. 常见问题

### 问题1: "invalid code" 错误

**症状**:
```json
{
  "code": 500,
  "message": "微信登录失败: invalid code"
}
```

**原因**:
- AppID或AppSecret配置错误
- code已失效（5分钟有效期）
- 网络问题无法访问微信服务器

**解决方案**:
1. 检查`.env`中的`WX_APPID`和`WX_SECRET`
2. 确认AppID与小程序`project.config.json`中一致
3. 重新点击登录获取新code
4. 检查服务器网络（能否访问`api.weixin.qq.com`）

### 问题2: "Token无效" 错误

**症状**:
```json
{
  "code": 401,
  "message": "Token无效"
}
```

**原因**:
- JWT_SECRET不一致
- Token格式错误
- Token已被篡改

**解决方案**:
1. 检查`.env`中的`JWT_SECRET`
2. 确认Authorization头格式: `Bearer <token>`
3. 重新登录获取新token

### 问题3: "未登录" 提示

**症状**: 访问需要登录的接口返回401

**原因**:
- Token未保存
- Token未携带到请求头
- Token已过期

**解决方案**:
1. 检查token是否保存:
   ```javascript
   console.log(wx.getStorageSync('token'))
   ```

2. 检查请求头:
   ```javascript
   // 在utils/request.js中
   header: {
     'Authorization': `Bearer ${app.globalData.token}`
   }
   ```

3. 重新登录

### 问题4: 授权窗口不弹出

**症状**: 点击登录没有反应

**原因**:
- `wx.getUserProfile` 必须由用户主动触发
- 只能在button的bindtap中调用

**解决方案**:
确保登录按钮在WXML中正确绑定:
```xml
<button bindtap="onLogin">立即登录</button>
```

### 问题5: 数据库连接失败

**症状**: 后端启动时报错

**原因**:
- 数据库未启动
- 数据库连接信息错误
- 数据表未创建

**解决方案**:
1. 启动MySQL服务
2. 检查`.env`中的数据库配置
3. 运行数据库初始化脚本

---

## 🎯 验证清单

配置完成后，请逐项验证:

### 后端验证
- [ ] `.env`文件已配置
- [ ] `WX_APPID`和`WX_SECRET`正确
- [ ] `JWT_SECRET`已设置（至少32位）
- [ ] 后端服务正常启动（端口3000）
- [ ] 数据库连接成功
- [ ] 路由已注册（auth、user）
- [ ] 中间件已创建（middleware/auth.js）

### 前端验证
- [ ] `project.config.json`中AppID已配置
- [ ] `app.js`中apiBase正确
- [ ] 微信开发者工具已配置（不校验域名）
- [ ] 项目编译无错误
- [ ] TabBar正常显示

### 功能验证
- [ ] 点击登录按钮有响应
- [ ] 授权窗口正常弹出
- [ ] 登录成功后显示用户信息
- [ ] Token正确保存到Storage
- [ ] 需要登录的接口可以访问
- [ ] 退出登录功能正常
- [ ] Token过期后自动清除

---

## 📚 相关文档

### 项目文档
- [登录功能实现方案.md](./小程序前端/登录功能实现方案.md) - 详细技术方案
- [API接口文档.md](./API接口文档.md) - 接口说明

### 微信官方文档
- [小程序登录](https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/login.html)
- [code2session API](https://developers.weixin.qq.com/miniprogram/dev/api-backend/open-api/login/auth.code2Session.html)
- [getUserProfile](https://developers.weixin.qq.com/miniprogram/dev/api/open-api/user-info/wx.getUserProfile.html)

### 第三方文档
- [JWT官方文档](https://jwt.io/)
- [jsonwebtoken npm](https://www.npmjs.com/package/jsonwebtoken)

---

## 🔒 安全提示

1. **敏感信息保护**
   - ✅ 不要将`.env`文件提交到Git
   - ✅ 不要在前端代码中硬编码AppSecret
   - ✅ 生产环境使用强随机的JWT_SECRET

2. **Token管理**
   - ✅ Token设置合理过期时间（推荐7天）
   - ✅ 重要操作需要二次验证
   - ✅ 退出登录时清除本地token

3. **生产环境**
   - ✅ 使用HTTPS协议
   - ✅ 配置正确的服务器域名
   - ✅ 启用请求频率限制
   - ✅ 记录操作日志

---

## 🆘 需要帮助？

如果按照本指南配置后仍有问题:

1. 查看Console错误信息
2. 检查后端日志
3. 参考"常见问题"章节
4. 查阅微信官方文档

---

**创建时间**: 2026年8月13日  
**适用项目**: 零食小程序商城  
**版本**: v1.0  
**更新**: 完整配置流程
