# 零食小程序商城 - 开发指南

## 📚 目录

1. [快速开始](#快速开始)
2. [开发环境搭建](#开发环境搭建)
3. [项目启动](#项目启动)
4. [开发流程](#开发流程)
5. [代码规范](#代码规范)
6. [常见问题](#常见问题)
7. [最佳实践](#最佳实践)

---

## 快速开始

### 项目概览

本项目是一个完整的零食小程序商城系统，包含：

- **小程序前端**：原生微信小程序
- **后端API**：Node.js + Express + MySQL
- **后台管理系统**：Vue 3 + Element Plus

### 技术栈要求

- Node.js >= 18.0
- MySQL >= 8.0
- 微信开发者工具
- VS Code（推荐）

---

## 开发环境搭建

### 1. 安装 Node.js

**Windows:**
1. 访问 [Node.js官网](https://nodejs.org/)
2. 下载 LTS 版本（18.x 或更高）
3. 运行安装程序
4. 验证安装：
```bash
node -v
npm -v
```

**注意**: 推荐使用 nvm 管理 Node.js 版本

### 2. 安装 MySQL

**Windows:**
1. 下载 [MySQL Installer](https://dev.mysql.com/downloads/installer/)
2. 选择 Developer Default 安装
3. 设置 root 密码
4. 启动 MySQL 服务

**验证安装**:
```bash
mysql -u root -p
```

### 3. 安装开发工具

**必需工具:**
- [微信开发者工具](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
- [VS Code](https://code.visualstudio.com/)

**VS Code 推荐插件:**
- Vetur / Volar（Vue 开发）
- ESLint（代码检查）
- Prettier（代码格式化）
- Chinese Language Pack（中文语言包）
- Path Intellisense（路径智能提示）

### 4. 配置 Git

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

---

## 项目启动

### 步骤 1：初始化数据库

```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库并导入数据
mysql -u root -p < database.sql

# 或者手动执行
CREATE DATABASE snack_mall;
USE snack_mall;
SOURCE database.sql;
```

### 步骤 2：启动后端 API

```bash
# 进入后端目录
cd 后端API

# 安装依赖
npm install

# 复制环境变量文件
cp .env.example .env

# 编辑 .env 文件，配置数据库连接
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=snack_mall
# DB_USER=root
# DB_PASSWORD=your_password

# 启动开发服务器
npm run dev
```

**验证**: 访问 http://localhost:3000/health 应该看到健康检查响应

### 步骤 3：启动后台管理系统

```bash
# 新开终端，进入后台目录
cd 后台管理系统

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

**验证**: 访问 http://localhost:8080 应该看到登录页面

**默认管理员账号**:
- 用户名: admin
- 密码: admin123

### 步骤 4：启动小程序前端

1. 打开微信开发者工具
2. 选择"导入项目"
3. 选择 `小程序前端` 目录
4. 填入 AppID（或使用测试号）
5. 点击"导入"

**配置 API 地址**:

编辑 `小程序前端/app.js`:
```javascript
globalData: {
  apiBase: 'http://localhost:3000/api'
}
```

**注意**: 
- 开发时需要在小程序开发工具中勾选"不校验合法域名"
- 真机调试需要配置合法域名

---

## 开发流程

### 工作流程

```
1. 创建功能分支
   ↓
2. 开发功能
   ↓
3. 本地测试
   ↓
4. 提交代码
   ↓
5. 代码审查
   ↓
6. 合并到主分支
   ↓
7. 部署上线
```

### Git 分支管理

**分支命名规范**:
```
master/main     - 生产环境
develop         - 开发环境
feature/xxx     - 新功能开发
bugfix/xxx      - Bug 修复
hotfix/xxx      - 紧急修复
```

**示例流程**:

```bash
# 1. 从 develop 创建功能分支
git checkout develop
git pull
git checkout -b feature/add-coupon-module

# 2. 开发并提交
git add .
git commit -m "feat: 添加优惠券模块"

# 3. 推送到远程
git push origin feature/add-coupon-module

# 4. 创建 Pull Request
# 在 GitHub/GitLab 上创建 PR，等待审查

# 5. 合并后删除分支
git checkout develop
git pull
git branch -d feature/add-coupon-module
```

### 提交信息规范

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
feat:     新功能
fix:      Bug 修复
docs:     文档更新
style:    代码格式调整（不影响功能）
refactor: 代码重构
perf:     性能优化
test:     测试相关
chore:    构建工具、辅助工具变动
```

**示例**:
```bash
git commit -m "feat: 添加商品搜索功能"
git commit -m "fix: 修复购物车数量显示错误"
git commit -m "docs: 更新 API 文档"
```

---

## 代码规范

### JavaScript/Vue 规范

**变量命名**:
```javascript
// 小驼峰
const userName = 'John'
const getUserInfo = () => {}

// 常量大写
const API_BASE_URL = 'https://api.example.com'
const MAX_PAGE_SIZE = 100

// 类名大驼峰
class UserService {}
```

**文件命名**:
```
组件: PascalCase    -> ProductCard.vue
页面: kebab-case    -> product-list.vue
工具: camelCase     -> request.js
```

**Vue 组件结构**:
```vue
<template>
  <!-- 模板 -->
</template>

<script setup>
// 1. 导入
import { ref, computed } from 'vue'

// 2. Props
const props = defineProps({
  title: String
})

// 3. Emits
const emit = defineEmits(['update'])

// 4. 响应式数据
const count = ref(0)

// 5. 计算属性
const doubleCount = computed(() => count.value * 2)

// 6. 方法
const increment = () => {
  count.value++
}

// 7. 生命周期
onMounted(() => {
  console.log('mounted')
})
</script>

<style lang="scss" scoped>
/* 样式 */
</style>
```

### 小程序规范

**页面结构**:
```javascript
Page({
  data: {
    // 数据
  },
  
  onLoad(options) {
    // 页面加载
  },
  
  onShow() {
    // 页面显示
  },
  
  // 自定义方法
  handleClick() {
    // 实现
  }
})
```

**命名规范**:
```javascript
// 页面/组件文件夹: kebab-case
pages/product-detail/

// 数据属性: camelCase
data: {
  productList: [],
  isLoading: false
}

// 方法: on + 动词
onProductTap() {}
onLoadMore() {}
```

### Node.js 规范

**模块导出**:
```javascript
// 单个导出
class UserController {
  async list(req, res) {
    // 实现
  }
}
module.exports = new UserController()

// 多个导出
module.exports = {
  getUser,
  updateUser,
  deleteUser
}
```

**异步处理**:
```javascript
// 推荐: async/await
async function getUser(id) {
  try {
    const user = await User.findByPk(id)
    return user
  } catch (error) {
    console.error(error)
    throw error
  }
}

// 避免: 回调地狱
```

### 数据库规范

**命名规范**:
```sql
-- 表名: 小写 + 下划线 + 复数
users, products, order_items

-- 字段名: 小写 + 下划线
user_id, created_at, is_active

-- 索引: idx_表名_字段名
idx_users_email, idx_products_category_id
```

**查询优化**:
```javascript
// ✅ 好的做法：只查询需要的字段
const user = await User.findByPk(id, {
  attributes: ['id', 'name', 'email']
})

// ❌ 避免：查询所有字段
const user = await User.findByPk(id)
```

---

## 常见问题

### 1. 数据库连接失败

**问题**: `SequelizeConnectionError: Access denied`

**解决方案**:
1. 检查 `.env` 文件中的数据库配置
2. 确认 MySQL 服务已启动
3. 验证用户名密码是否正确
4. 检查数据库是否存在

```bash
# 测试连接
mysql -h localhost -u root -p
```

### 2. 端口被占用

**问题**: `Error: listen EADDRINUSE: address already in use :::3000`

**解决方案**:
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# 或修改端口
# 在 .env 中修改 PORT=3001
```

### 3. npm 安装依赖慢

**解决方案**:
```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 或使用 pnpm/yarn
npm install -g pnpm
pnpm install
```

### 4. 微信小程序请求失败

**问题**: `request:fail url not in domain list`

**解决方案**:
- 开发时：勾选"不校验合法域名"
- 生产环境：在小程序后台配置合法域名

### 5. JWT Token 过期

**问题**: `401 Unauthorized`

**解决方案**:
1. 检查 token 是否正确传递
2. 检查 token 是否过期（默认7天）
3. 重新登录获取新 token

### 6. CORS 跨域问题

**解决方案**:

后端已配置 CORS，如果仍有问题：

```javascript
// 后端 app.js
app.use(cors({
  origin: ['http://localhost:8080'],
  credentials: true
}))
```

---

## 最佳实践

### 1. 错误处理

**统一错误处理**:
```javascript
// 后端
try {
  const data = await someAsyncOperation()
  return Response.success(res, data)
} catch (error) {
  logger.error('操作失败:', error)
  return Response.error(res, '操作失败', 500)
}

// 前端
try {
  const data = await api.getProducts()
  // 处理数据
} catch (error) {
  ElMessage.error(error.message || '获取数据失败')
}
```

### 2. 性能优化

**前端优化**:
```javascript
// 1. 路由懒加载
{
  path: '/products',
  component: () => import('@/views/Products.vue')
}

// 2. 图片懒加载
<image lazy-load="true" src="{{imageUrl}}" />

// 3. 列表分页
const loadMore = () => {
  if (hasMore) {
    page++
    loadProducts()
  }
}
```

**后端优化**:
```javascript
// 1. 使用索引
await Product.findAll({
  where: { status: 1 }, // status 字段应有索引
  limit: 10
})

// 2. 避免 N+1 查询
await Product.findAll({
  include: [{
    model: Category,
    as: 'category'
  }]
})

// 3. 使用缓存
const cached = await redis.get(`product:${id}`)
if (cached) return JSON.parse(cached)
```

### 3. 安全实践

**输入验证**:
```javascript
// 使用 express-validator
const { body, validationResult } = require('express-validator')

router.post('/products',
  body('name').notEmpty().withMessage('商品名称不能为空'),
  body('price').isFloat({ min: 0 }).withMessage('价格必须大于0'),
  async (req, res) => {
    const errors = validationResult(req)
    if (!errors.isEmpty()) {
      return Response.error(res, errors.array()[0].msg, 400)
    }
    // 处理请求
  }
)
```

**密码处理**:
```javascript
const bcrypt = require('bcryptjs')

// 加密
const hashedPassword = await bcrypt.hash(password, 10)

// 验证
const isValid = await bcrypt.compare(password, hashedPassword)
```

### 4. 日志记录

**结构化日志**:
```javascript
logger.info('用户登录', {
  userId: user.id,
  ip: req.ip,
  userAgent: req.headers['user-agent']
})

logger.error('订单创建失败', {
  userId: user.id,
  error: error.message,
  stack: error.stack
})
```

### 5. 代码复用

**创建通用组件**:
```vue
<!-- components/ProductCard.vue -->
<template>
  <div class="product-card">
    <img :src="product.cover" />
    <h3>{{ product.name }}</h3>
    <p class="price">¥{{ product.price }}</p>
  </div>
</template>

<script setup>
const props = defineProps({
  product: {
    type: Object,
    required: true
  }
})
</script>
```

**创建工具函数**:
```javascript
// utils/format.js
export function formatPrice(price) {
  return parseFloat(price).toFixed(2)
}

export function formatDate(date) {
  return dayjs(date).format('YYYY-MM-DD HH:mm:ss')
}
```

### 6. 测试

**单元测试示例**:
```javascript
// __tests__/user.test.js
const { User } = require('../models')

describe('User Model', () => {
  test('should create user', async () => {
    const user = await User.create({
      openid: 'test_openid',
      nickname: 'Test User'
    })
    
    expect(user.openid).toBe('test_openid')
    expect(user.nickname).toBe('Test User')
  })
})
```

### 7. 文档维护

**保持文档更新**:
- API 变更时更新接口文档
- 新功能添加使用说明
- 记录重要的技术决策
- 维护 CHANGELOG

---

## 开发工具推荐

### Chrome 扩展
- Vue.js devtools
- React Developer Tools（如使用 Taro）
- Postman（API 测试）

### VS Code 设置

```json
{
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000
}
```

### 调试技巧

**后端调试**:
```javascript
// 使用 debugger
debugger;

// 使用 console
console.log('User:', user)
console.table(products)

// VS Code 调试配置
{
  "type": "node",
  "request": "launch",
  "name": "Debug API",
  "program": "${workspaceFolder}/src/app.js"
}
```

**小程序调试**:
- 使用开发者工具 Console
- 使用 wx.showToast 调试
- 使用真机调试查看网络请求

---

## 持续学习

### 推荐资源

**官方文档**:
- [Vue 3 文档](https://cn.vuejs.org/)
- [微信小程序文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- [Node.js 文档](https://nodejs.org/docs/)
- [Express 文档](https://expressjs.com/)
- [Sequelize 文档](https://sequelize.org/)

**学习路径**:
1. 先掌握基础（JavaScript、HTML、CSS）
2. 学习框架核心概念
3. 实践小项目
4. 阅读优秀开源项目
5. 参与社区讨论

---

**文档版本**: v1.0  
**最后更新**: 2026-08-12  
**维护者**: 开发团队

如有问题，请在项目 Issues 中提出。
