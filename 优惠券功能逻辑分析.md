# 优惠券功能完整逻辑分析

## 一、数据库设计

### 1. 优惠券表 (coupons)
```sql
- id: 优惠券ID
- name: 优惠券名称
- type: 优惠券类型（通用券、商品券等）
- discount_type: 折扣类型（1:固定金额 2:折扣率）
- discount_value: 折扣值（固定金额或折扣率，如8.5表示8.5折）
- min_amount: 最低使用金额
- total_count: 发放总数（0表示不限量）
- receive_count: 已领取数量
- per_limit: 每人限领数量
- start_time: 有效期开始
- end_time: 有效期结束
- status: 状态（1:启用 0:禁用）
```

### 2. 用户优惠券表 (user_coupons)
```sql
- id: 用户优惠券ID
- user_id: 用户ID
- coupon_id: 优惠券ID（关联coupons表）
- status: 状态（0:未使用 1:已使用 2:已过期）
- receive_time: 领取时间
- use_time: 使用时间
- expire_time: 过期时间（继承自优惠券的end_time）
- order_id: 关联的订单ID（使用时记录）
```

### 3. 订单表 (orders) - 优惠券相关字段
```sql
- coupon_id: 使用的用户优惠券ID（user_coupons.id）
- total_amount: 商品总价
- discount_amount: 优惠金额（优惠券+积分等）
- freight_amount: 运费
- pay_amount: 实付金额 = total_amount - discount_amount + freight_amount
```

---

## 二、页面功能与逻辑

### 1. 首页 (index)

#### 功能
- 展示系统可领取的优惠券（最多5张）
- 用户可以点击"领取"按钮领取优惠券

#### 数据流
```
1. loadCoupons() 
   ↓
2. 检查用户登录状态
   ↓（未登录返回空数组）
3. GET /api/coupons/available
   ↓
4. 返回可领取的优惠券列表（status=1的券）
   ↓
5. 页面展示优惠券卡片（显示金额、名称、使用门槛、有效期）
```

#### 领取逻辑
```javascript
receiveCoupon(优惠券ID)
  ↓
检查登录 → 未登录跳转登录
  ↓
POST /api/coupons/receive
  ↓
后端验证：
  - 优惠券是否存在且启用
  - 用户是否已达领取上限（per_limit）
  - 优惠券库存是否充足（total_count）
  ↓
创建 user_coupons 记录
  - status: 0（未使用）
  - expire_time: 继承优惠券的end_time
  ↓
更新优惠券领取数量（receive_count + 1）
  ↓
返回成功 → 刷新优惠券列表
```

#### 当前问题
- TODO标记：`receiveCoupon()` 中的API调用未实现，需要调用 `app.api.coupon.receiveCoupon(id)`

---

### 2. 我的优惠券 (coupon-list)

#### 功能
- Tab切换：未使用 / 已使用 / 已过期
- 展示用户拥有的优惠券
- 点击"使用"跳转到商品分类页

#### 数据流
```
1. switchTab(tab) → activeTab = 0/1/2
   ↓
2. loadCoupons()
   ↓
3. GET /api/coupons/my?status={activeTab}
   ↓
4. 查询 user_coupons 表（关联 coupons 表）
   - WHERE user_id = 当前用户 AND status = {activeTab}
   ↓
5. 返回优惠券列表（包含优惠券详情）
   ↓
6. 页面展示：
   - 未使用：显示"使用"按钮
   - 已使用：显示使用时间
   - 已过期：灰色显示
```

#### 状态判断
```javascript
// 后端自动判断过期
如果 user_coupons.expire_time < NOW() 且 status = 0
  → 自动更新为 status = 2（已过期）

// 前端显示逻辑
status: 0 → 未使用（可用）
status: 1 → 已使用（关联订单）
status: 2 → 已过期
```

---

### 3. 订单确认页 (order-confirm)

#### 功能
- 显示优惠券入口（显示已选优惠券或"请选择"）
- 点击跳转到优惠券选择页
- 实时计算显示：商品总价、优惠券折扣、实付款

#### 数据流
```
1. 初始化
   ↓
   data: {
     couponId: null,           // 选中的用户优惠券ID
     couponDiscount: 0,        // 优惠金额
     totalPrice: 0,            // 商品总价
     finalPrice: 0             // 实付款
   }

2. calculatePrice()
   ↓
   totalPrice = ∑(商品单价 × 数量)
   finalPrice = totalPrice - couponDiscount - pointsDiscount

3. 选择优惠券
   onSelectCoupon()
     ↓
   跳转到 coupon-select 页面
     - 传递参数: orderAmount={totalPrice}, currentCouponId={couponId}
     ↓
   优惠券选择页返回（通过页面栈修改前一页data）
     ↓
   prevPage.setData({
     couponId: 选中的ID,
     couponDiscount: 优惠金额
   })
     ↓
   calculatePrice() 重新计算价格

4. 提交订单
   onSubmit()
     ↓
   POST /api/orders
   Body: {
     items: [...],
     coupon_id: this.data.couponId,  // 用户优惠券ID
     ...
   }
```

#### 价格计算公式
```javascript
商品总价 = ∑(商品单价 × 数量)
优惠券折扣 = calculateCouponDiscount(couponId, 商品总价)
积分抵扣 = pointsUsed × 积分汇率
运费 = 0（暂时免运费）
实付款 = 商品总价 - 优惠券折扣 - 积分抵扣 + 运费
```

---

### 4. 优惠券选择页 (coupon-select)

#### 功能
- 根据订单金额筛选可用/不可用优惠券
- 实时计算每张优惠券的优惠金额
- 选择优惠券并返回到订单确认页

#### 数据流
```
1. onLoad(options)
   ↓
   接收参数:
     - orderAmount: 订单金额
     - currentCouponId: 当前已选优惠券ID（可选）

2. loadCoupons()
   ↓
   GET /api/coupons/my?status=0
   ↓
   获取所有未使用的优惠券

3. 本地筛选和计算
   ↓
   for each coupon:
     ↓
     检查是否过期
       expire_time < NOW() → unavailable（已过期）
     ↓
     检查金额门槛
       orderAmount < min_amount → unavailable（订单需满XX元）
     ↓
     计算优惠金额
       if discount_type === 1: // 固定金额
         discountAmount = discount_value
       if discount_type === 2: // 折扣
         discountRate = discount_value / 10  // 8.5折 = 0.85
         discountAmount = orderAmount × (1 - discountRate)
     ↓
     available.push({
       ...coupon,
       discount_amount: discountAmount  // 本次可优惠金额
     })

4. 排序
   ↓
   available.sort((a, b) => b.discount_amount - a.discount_amount)
   // 按优惠金额从大到小排序

5. 用户选择
   onSelectCoupon(couponId, discountAmount)
     ↓
   this.setData({
     selectedCouponId: couponId,
     selectedDiscount: discountAmount
   })

6. 确认选择
   onConfirm()
     ↓
   获取前一个页面（订单确认页）
     ↓
   prevPage.setData({
     couponId: selectedCouponId,
     couponDiscount: selectedDiscount
   })
     ↓
   prevPage.calculatePrice()  // 触发重新计算
     ↓
   wx.navigateBack()  // 返回
```

#### 可用性判断规则
```javascript
优惠券可用的条件（同时满足）：
1. status === 0（未使用）
2. expire_time >= NOW()（未过期）
3. orderAmount >= min_amount（满足使用门槛）
```

#### 优惠金额计算
```javascript
// 类型1: 固定金额折扣
discount_type === 1
  优惠金额 = discount_value
  示例: "满100减10" → 优惠10元

// 类型2: 折扣率
discount_type === 2
  折扣率 = discount_value / 10
  优惠金额 = orderAmount × (1 - 折扣率)
  示例: "8.5折券", orderAmount=100 
    → 折扣率=0.85, 优惠金额=100×(1-0.85)=15元
```

---

### 5. 订单详情页 (order-detail)

#### 功能
- 显示订单使用的优惠券信息
- 展示费用明细（商品总价、优惠券折扣、实付款）

#### 数据展示
```
loadOrderDetail(orderId)
  ↓
GET /api/orders/:id
  ↓
返回订单数据:
  {
    total_amount: 商品总价,
    discount_amount: 优惠券折扣,
    freight_amount: 运费,
    pay_amount: 实付金额,
    coupon_id: 使用的优惠券ID,
    ...
  }
  ↓
前端字段映射:
  order.product_amount = order.total_amount
  order.coupon_discount = order.discount_amount
  order.delivery_fee = order.freight_amount
  order.total_amount = order.pay_amount  // 实付款
  ↓
WXML 显示:
  商品总价: ¥{{order.product_amount}}
  配送费: ¥{{order.delivery_fee}}
  优惠券: -¥{{order.coupon_discount}}  (红色显示)
  实付款: ¥{{order.total_amount}}  (大号加粗)
```

---

### 6. 订单列表页 (order-list)

#### 功能
- 显示订单的实付金额（而非商品总价）

#### 显示逻辑
```
订单卡片显示:
  共{item_count}件商品 实付：¥{{item.pay_amount}}
  
注意：
  ❌ 错误: 显示 total_amount（商品总价）
  ✅ 正确: 显示 pay_amount（实付金额）
```

---

## 三、后端接口逻辑

### 1. 创建订单时处理优惠券

```javascript
POST /api/orders
Body: {
  coupon_id: 用户优惠券ID,
  items: [...],
  ...
}

// 后端处理流程
1. 验证优惠券
   ↓
   查询 user_coupons (id = coupon_id, user_id = 当前用户)
     ↓
   检查:
     - 是否存在
     - status === 0（未使用）
     - expire_time >= NOW()（未过期）
     - 关联的 coupon 记录存在
     ↓
   检查使用门槛:
     if (商品总价 < coupon.min_amount)
       → 返回错误: "订单金额需满XX元"

2. 计算优惠金额
   ↓
   if (coupon.discount_type === 1):
     couponDiscount = coupon.discount_value
   
   if (coupon.discount_type === 2):
     discountRate = coupon.discount_value / 10
     couponDiscount = 商品总价 × (1 - discountRate)
   
   // 防止优惠超过商品总价
   couponDiscount = min(couponDiscount, 商品总价)

3. 创建订单
   ↓
   Order.create({
     user_id: 当前用户,
     total_amount: 商品总价,
     discount_amount: couponDiscount,
     freight_amount: 运费,
     pay_amount: 商品总价 - couponDiscount + 运费,
     coupon_id: coupon_id,  // 记录使用的优惠券
     ...
   })

4. 标记优惠券为已使用
   ↓
   UserCoupon.update({
     status: 1,           // 已使用
     use_time: NOW(),
     order_id: 新订单ID
   })

5. 扣减库存、清空购物车等
   ...

6. 提交事务
```

### 2. 取消订单时恢复优惠券

```javascript
// 在 cancel-unpaid-orders.js 或手动取消订单时

if (order.coupon_id) {
  // 恢复优惠券为未使用状态
  await UserCoupon.update({
    status: 0,           // 未使用
    use_time: null,
    order_id: null
  }, {
    where: { id: order.coupon_id }
  })
}
```

---

## 四、关键业务规则

### 1. 优惠券领取规则
- ✅ 同一优惠券可重复领取（受 per_limit 限制）
- ✅ 领取后立即生效，过期时间继承优惠券的 end_time
- ✅ 库存限制：total_count > 0 时检查 receive_count

### 2. 优惠券使用规则
- ✅ 必须满足最低金额门槛（min_amount）
- ✅ 必须在有效期内（expire_time）
- ✅ 一个订单只能使用一张优惠券
- ✅ 优惠金额不能超过商品总价

### 3. 优惠券状态流转
```
未使用(0) → 已使用(1)  [订单创建成功]
         → 已过期(2)  [expire_time < NOW()]

已使用(1) → 未使用(0)  [订单取消/超时取消]
```

### 4. 订单金额计算
```
商品总价 = ∑(商品单价 × 数量)

优惠券折扣 = 
  if discount_type = 1: discount_value
  if discount_type = 2: 商品总价 × (1 - discount_value/10)

实付金额 = 商品总价 - 优惠券折扣 - 积分抵扣 + 运费
         = total_amount - discount_amount + freight_amount
```

---

## 五、当前存在的问题

### 1. ❌ 首页领取优惠券功能未实现
**位置**: `pages/index/index.js` 第362行  
**问题**: `receiveCoupon()` 方法中有TODO标记，未调用API  
**修复**: 
```javascript
async receiveCoupon(e) {
  const { id } = e.currentTarget.dataset
  
  if (!app.store.isLoggedIn()) {
    errorHandler.handle(new Error('请先登录'), { code: 'NOT_LOGGED_IN' })
    return
  }

  try {
    await app.api.coupon.receiveCoupon(id)
    errorHandler.showSuccess('领取成功')
    this.loadCoupons()
  } catch (error) {
    // 错误已统一处理
  }
}
```

### 2. ❌ API接口路径不一致
**位置**: `services/api/coupon.js`  
**问题**: 
- 前端调用: `/coupons/${couponId}/receive`
- 后端路由: `/coupons/receive`（Body中传coupon_id）

**修复方案A（推荐）**: 修改后端路由
```javascript
// 后端 routes/coupon.js
router.post('/:id/receive', authenticateToken, async (req, res) => {
  const coupon_id = req.params.id
  // ... 原有逻辑
})
```

**修复方案B**: 修改前端API调用
```javascript
// 前端 services/api/coupon.js
function receiveCoupon(couponId) {
  return request.post('/coupons/receive', { coupon_id: couponId })
}
```

### 3. ⚠️ 优惠券选择页字段引用
**位置**: `coupon-select.wxml` 第21行  
**已修复**: `item.min_amount` → `item.coupon.min_amount`

### 4. ⚠️ 订单列表显示金额错误
**位置**: `order-list.wxml` 第50行  
**已修复**: `item.total_amount` → `item.pay_amount`

---

## 六、完整的数据流图

```
[系统优惠券] → [用户领取] → [用户优惠券] → [订单使用] → [订单记录]
     ↓              ↓              ↓             ↓            ↓
  coupons      领取接口     user_coupons    创建订单       orders
  (status=1)     ↓           (status=0)        ↓         (coupon_id)
               receive       expire_time      标记已使用      ↓
                ↓               ↓           (status=1)     恢复优惠券
              增加计数      选择可用券       use_time     (订单取消时)
           receive_count        ↓           order_id
                             计算优惠
                          discount_amount
```

---

## 七、测试场景

### 场景1: 正常使用流程
1. 用户在首页领取"满100减10"优惠券
2. 加入商品到购物车，商品总价120元
3. 结算时选择优惠券，显示可优惠10元，实付110元
4. 提交订单成功
5. 订单详情显示：商品总价120元，优惠券-10元，实付110元
6. 优惠券状态变为"已使用"

### 场景2: 金额不满足
1. 用户拥有"满200减30"优惠券
2. 商品总价150元
3. 优惠券选择页显示该券为"不可用：订单需满200元"
4. 无法选择该优惠券

### 场景3: 订单取消恢复
1. 用户创建订单使用优惠券
2. 未支付，15分钟后系统自动取消订单
3. 优惠券自动恢复为"未使用"状态
4. 用户可以再次使用该优惠券

### 场景4: 折扣券计算
1. 用户拥有"8.5折优惠券"（discount_type=2, discount_value=8.5）
2. 商品总价100元
3. 优惠金额 = 100 × (1 - 8.5/10) = 100 × 0.15 = 15元
4. 实付85元

---

## 八、优化建议

### 1. 前端优化
- [ ] 实现首页领取优惠券功能
- [ ] 添加优惠券过期倒计时显示
- [ ] 优惠券选择页添加搜索/筛选功能
- [ ] 添加优惠券使用记录查看

### 2. 后端优化
- [ ] 统一API路径规范（RESTful风格）
- [ ] 添加优惠券使用统计接口
- [ ] 实现优惠券批量发放（营销活动）
- [ ] 添加优惠券组合使用限制

### 3. 业务优化
- [ ] 支持商品指定优惠券（而非全场通用）
- [ ] 支持新人专享、会员专享等标签
- [ ] 支持优惠券叠加使用规则配置
- [ ] 添加优惠券分享功能

---

## 总结

优惠券功能涉及 **6个页面** 和 **多个接口**，核心逻辑集中在：
1. **领取**: 首页领取 → user_coupons表创建记录
2. **选择**: 订单确认页选择 → 根据金额筛选可用券
3. **使用**: 提交订单 → 计算折扣、标记已使用
4. **恢复**: 订单取消 → 优惠券状态恢复未使用

当前主要需要修复首页领取功能的API调用，其他核心逻辑已基本完善。