# 零食小程序商城 - 部署文档

## 📋 目录

1. [部署前准备](#部署前准备)
2. [服务器选择](#服务器选择)
3. [环境配置](#环境配置)
4. [后端部署](#后端部署)
5. [后台管理系统部署](#后台管理系统部署)
6. [小程序发布](#小程序发布)
7. [域名和SSL配置](#域名和ssl配置)
8. [监控和维护](#监控和维护)

---

## 部署前准备

### 1. 必需资源

**服务器资源**:
- 云服务器（推荐配置：2核4G，带宽3M）
- MySQL数据库（可与应用同服务器）
- 域名（已备案）
- SSL证书

**第三方服务**:
- 微信小程序账号（已认证）
- 对象存储 OSS（阿里云/腾讯云）
- 微信支付商户号（可选）

**开发账号**:
- 服务器SSH访问权限
- 域名DNS管理权限
- 小程序管理员权限

### 2. 检查清单

部署前确认：

- [ ] 服务器已购买并可访问
- [ ] 域名已备案并解析到服务器
- [ ] SSL证书已申请
- [ ] 数据库已创建
- [ ] 小程序AppID已获取
- [ ] 代码已通过测试
- [ ] 环境变量已配置
- [ ] 备份策略已制定

---

## 服务器选择

### 方案对比

| 方案 | 成本 | 适用场景 | 优点 | 缺点 |
|------|------|----------|------|------|
| 阿里云ECS | 100-500元/月 | 中小型项目 | 稳定性好，生态完善 | 价格较高 |
| 腾讯云CVM | 100-500元/月 | 中小型项目 | 微信生态集成好 | 文档相对少 |
| Serverless | 20-100元/月 | 初创项目 | 按量付费，成本低 | 功能限制 |
| Docker + K8s | 500+元/月 | 大型项目 | 易扩展，自动化 | 复杂度高 |

### 推荐配置

**初期（MVP阶段）**:
```
配置: 2核4G
带宽: 3M
系统: Ubuntu 22.04 LTS
区域: 根据用户分布选择
月成本: 约120元
```

**成长期（用户增长）**:
```
配置: 4核8G
带宽: 5M
系统: Ubuntu 22.04 LTS
额外: Redis、OSS
月成本: 约300元
```

**成熟期（稳定运营）**:
```
配置: 8核16G
带宽: 10M
架构: 负载均衡 + 双服务器
数据库: 独立RDS
月成本: 约1000元
```

---

## 环境配置

### 1. 服务器初始化

```bash
# SSH登录服务器
ssh root@your-server-ip

# 更新系统
apt update && apt upgrade -y

# 创建部署用户
adduser deploy
usermod -aG sudo deploy

# 配置SSH密钥登录（推荐）
ssh-copy-id deploy@your-server-ip

# 切换到部署用户
su - deploy
```

### 2. 安装 Node.js

```bash
# 安装 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# 加载 nvm
source ~/.bashrc

# 安装 Node.js 18
nvm install 18
nvm use 18
nvm alias default 18

# 验证
node -v
npm -v
```

### 3. 安装 MySQL

```bash
# 安装 MySQL
sudo apt install mysql-server -y

# 安全配置
sudo mysql_secure_installation

# 创建数据库和用户
sudo mysql -u root -p

CREATE DATABASE snack_mall CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'snackadmin'@'localhost' IDENTIFIED BY 'strong_password_here';
GRANT ALL PRIVILEGES ON snack_mall.* TO 'snackadmin'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# 导入数据库
mysql -u snackadmin -p snack_mall < database.sql
```

### 4. 安装 Nginx

```bash
# 安装 Nginx
sudo apt install nginx -y

# 启动 Nginx
sudo systemctl start nginx
sudo systemctl enable nginx

# 检查状态
sudo systemctl status nginx
```

### 5. 安装 PM2

```bash
# 全局安装 PM2
npm install -g pm2

# 设置开机自启
pm2 startup
```

---

## 后端部署

### 1. 上传代码

**方式一：使用 Git**

```bash
# 在服务器上克隆代码
cd /var/www
git clone https://github.com/your-username/snack-mall-api.git
cd snack-mall-api
```

**方式二：使用 SCP**

```bash
# 本地打包
tar -czf api.tar.gz 后端API/

# 上传到服务器
scp api.tar.gz deploy@your-server:/var/www/

# 服务器解压
ssh deploy@your-server
cd /var/www
tar -xzf api.tar.gz
mv 后端API snack-mall-api
```

### 2. 安装依赖

```bash
cd /var/www/snack-mall-api

# 安装生产依赖
npm install --production

# 或使用 pnpm（更快）
npm install -g pnpm
pnpm install --prod
```

### 3. 配置环境变量

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑环境变量
nano .env
```

生产环境配置：
```env
NODE_ENV=production
PORT=3000
API_PREFIX=/api

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=snack_mall
DB_USER=snackadmin
DB_PASSWORD=strong_password_here

# JWT密钥（必须修改）
JWT_SECRET=your-production-secret-key-change-this

# 微信配置
WX_APPID=your-wechat-appid
WX_SECRET=your-wechat-secret

# 微信支付配置
WX_PAY_MCHID=your-merchant-id
WX_PAY_KEY=your-payment-key

# 文件上传
UPLOAD_PATH=/var/www/snack-mall-api/uploads

# 日志
LOG_LEVEL=error
LOG_PATH=/var/www/snack-mall-api/logs
```

### 4. 使用 PM2 启动

```bash
# 启动应用
pm2 start src/app.js --name snack-api

# 查看状态
pm2 status

# 查看日志
pm2 logs snack-api

# 设置自动重启
pm2 save
```

**PM2 配置文件** (ecosystem.config.js):

```javascript
module.exports = {
  apps: [{
    name: 'snack-api',
    script: './src/app.js',
    instances: 2,  // 使用2个实例
    exec_mode: 'cluster',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    error_file: './logs/pm2-error.log',
    out_file: './logs/pm2-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss',
    max_memory_restart: '500M',
    autorestart: true,
    watch: false
  }]
}

// 使用配置文件启动
pm2 start ecosystem.config.js
```

### 5. 配置 Nginx 反向代理

```bash
# 创建配置文件
sudo nano /etc/nginx/sites-available/snack-api
```

```nginx
server {
    listen 80;
    server_name api.yourdomain.com;

    # 日志
    access_log /var/log/nginx/snack-api-access.log;
    error_log /var/log/nginx/snack-api-error.log;

    # API 代理
    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # 静态文件
    location /uploads {
        alias /var/www/snack-mall-api/uploads;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

```bash
# 启用站点
sudo ln -s /etc/nginx/sites-available/snack-api /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启 Nginx
sudo systemctl reload nginx
```

---

## 后台管理系统部署

### 1. 本地构建

```bash
# 在本地项目目录
cd 后台管理系统

# 安装依赖
npm install

# 生产构建
npm run build

# 构建产物在 dist/ 目录
```

### 2. 上传到服务器

```bash
# 打包 dist 目录
tar -czf admin-dist.tar.gz dist/

# 上传
scp admin-dist.tar.gz deploy@your-server:/var/www/

# 服务器解压
ssh deploy@your-server
cd /var/www
mkdir snack-admin
tar -xzf admin-dist.tar.gz -C snack-admin
```

### 3. 配置 Nginx

```bash
sudo nano /etc/nginx/sites-available/snack-admin
```

```nginx
server {
    listen 80;
    server_name admin.yourdomain.com;

    root /var/www/snack-admin/dist;
    index index.html;

    # 日志
    access_log /var/log/nginx/snack-admin-access.log;
    error_log /var/log/nginx/snack-admin-error.log;

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;
    gzip_min_length 1000;

    # SPA 路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

```bash
# 启用站点
sudo ln -s /etc/nginx/sites-available/snack-admin /etc/nginx/sites-enabled/

# 重启 Nginx
sudo nginx -t
sudo systemctl reload nginx
```

---

## 小程序发布

### 1. 配置服务器域名

登录[微信公众平台](https://mp.weixin.qq.com/)：

```
开发 -> 开发管理 -> 开发设置 -> 服务器域名

配置以下域名：
- request合法域名: https://api.yourdomain.com
- uploadFile合法域名: https://api.yourdomain.com
- downloadFile合法域名: https://api.yourdomain.com
```

### 2. 修改小程序配置

编辑 `app.js`:
```javascript
globalData: {
  apiBase: 'https://api.yourdomain.com/api'
}
```

### 3. 上传代码

1. 打开微信开发者工具
2. 点击"上传"按钮
3. 填写版本号（如：1.0.0）
4. 填写项目备注
5. 点击"上传"

### 4. 提交审核

1. 登录微信公众平台
2. 进入"版本管理"
3. 选择刚上传的版本
4. 点击"提交审核"
5. 填写审核信息：
   - 功能介绍
   - 测试账号
   - 补充说明

### 5. 发布上线

审核通过后：
1. 进入"版本管理"
2. 点击"发布"
3. 确认发布

**注意事项**:
- 首次发布审核时间较长（3-7天）
- 后续更新通常1-3天
- 准备好测试账号和测试流程
- 确保所有功能正常

---

## 域名和SSL配置

### 1. 域名解析

在域名服务商添加DNS记录：

```
类型    主机记录    记录值
A       api        your-server-ip
A       admin      your-server-ip
A       www        your-server-ip
```

### 2. 申请SSL证书

**方式一：Let's Encrypt（免费）**

```bash
# 安装 Certbot
sudo apt install certbot python3-certbot-nginx -y

# 申请证书
sudo certbot --nginx -d api.yourdomain.com
sudo certbot --nginx -d admin.yourdomain.com

# 自动续期
sudo certbot renew --dry-run
```

**方式二：阿里云/腾讯云（免费）**

1. 登录云服务商控制台
2. 进入SSL证书服务
3. 申请免费DV证书
4. 下载Nginx证书文件
5. 上传到服务器

### 3. 配置HTTPS

```nginx
server {
    listen 443 ssl http2;
    server_name api.yourdomain.com;

    # SSL 证书
    ssl_certificate /etc/letsencrypt/live/api.yourdomain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.yourdomain.com/privkey.pem;

    # SSL 配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # ... 其他配置
}

# HTTP 重定向到 HTTPS
server {
    listen 80;
    server_name api.yourdomain.com;
    return 301 https://$server_name$request_uri;
}
```

---

## 监控和维护

### 1. 日志管理

**查看日志**:
```bash
# PM2 日志
pm2 logs snack-api

# Nginx 日志
tail -f /var/log/nginx/snack-api-access.log
tail -f /var/log/nginx/snack-api-error.log

# 系统日志
journalctl -u nginx -f
```

**日志轮转**:
```bash
# 配置日志轮转
sudo nano /etc/logrotate.d/snack-api
```

```
/var/www/snack-mall-api/logs/*.log {
    daily
    rotate 14
    compress
    delaycompress
    notifempty
    create 0640 deploy deploy
    sharedscripts
    postrotate
        pm2 reloadLogs
    endscript
}
```

### 2. 性能监控

**使用 PM2 监控**:
```bash
# 实时监控
pm2 monit

# Web 界面监控（需要注册）
pm2 link <secret> <public>
```

**使用 htop 监控系统**:
```bash
sudo apt install htop
htop
```

### 3. 数据库备份

**自动备份脚本**:

```bash
# 创建备份脚本
nano /home/deploy/backup-db.sh
```

```bash
#!/bin/bash
BACKUP_DIR="/home/deploy/backups"
DATE=$(date +%Y%m%d_%H%M%S)
DB_NAME="snack_mall"
DB_USER="snackadmin"
DB_PASS="your_password"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
mysqldump -u$DB_USER -p$DB_PASS $DB_NAME | gzip > $BACKUP_DIR/snack_mall_$DATE.sql.gz

# 删除7天前的备份
find $BACKUP_DIR -name "*.sql.gz" -mtime +7 -delete

echo "Backup completed: snack_mall_$DATE.sql.gz"
```

```bash
# 添加执行权限
chmod +x /home/deploy/backup-db.sh

# 添加到 crontab（每天凌晨2点备份）
crontab -e
0 2 * * * /home/deploy/backup-db.sh >> /home/deploy/backup.log 2>&1
```

### 4. 应用更新

**零停机更新流程**:

```bash
# 1. 拉取最新代码
cd /var/www/snack-mall-api
git pull origin main

# 2. 安装依赖（如有变化）
npm install --production

# 3. 运行数据库迁移（如有）
npm run migrate

# 4. 重启应用
pm2 reload snack-api

# 5. 查看状态
pm2 status
pm2 logs snack-api --lines 100
```

### 5. 故障恢复

**应用崩溃**:
```bash
# 查看日志
pm2 logs snack-api --err

# 重启应用
pm2 restart snack-api

# 如果反复崩溃，回滚代码
git checkout <previous-commit>
pm2 restart snack-api
```

**数据库问题**:
```bash
# 检查 MySQL 状态
sudo systemctl status mysql

# 重启 MySQL
sudo systemctl restart mysql

# 恢复备份
gunzip < backup.sql.gz | mysql -u root -p snack_mall
```

**磁盘空间不足**:
```bash
# 检查磁盘使用
df -h

# 清理日志
pm2 flush
sudo journalctl --vacuum-time=7d

# 清理旧备份
find /home/deploy/backups -mtime +30 -delete
```

---

## 成本估算

### 最小配置（适合MVP）

| 项目 | 配置 | 月费用 |
|------|------|--------|
| 云服务器 | 2核4G | ¥120 |
| 域名 | .com | ¥70/年 |
| SSL证书 | Let's Encrypt | ¥0 |
| OSS存储 | 10GB | ¥2 |
| **合计** | - | **约¥128/月** |

### 标准配置（适合正式运营）

| 项目 | 配置 | 月费用 |
|------|------|--------|
| 云服务器 | 4核8G | ¥300 |
| RDS数据库 | 2核4G | ¥200 |
| Redis | 1GB | ¥100 |
| OSS存储 | 50GB | ¥10 |
| CDN流量 | 100GB | ¥30 |
| 短信服务 | 1000条 | ¥40 |
| **合计** | - | **约¥680/月** |

---

## 检查清单

部署完成后检查：

### 后端API
- [ ] API能正常访问（https://api.yourdomain.com/health）
- [ ] 数据库连接正常
- [ ] JWT认证工作正常
- [ ] 文件上传功能正常
- [ ] PM2自动重启配置正常
- [ ] 日志正常记录

### 后台管理
- [ ] 能够访问（https://admin.yourdomain.com）
- [ ] 登录功能正常
- [ ] 所有页面加载正常
- [ ] API请求正常
- [ ] 静态资源加载正常

### 小程序
- [ ] 能够正常打开
- [ ] 登录功能正常
- [ ] 商品列表加载正常
- [ ] 购物车功能正常
- [ ] 下单流程正常
- [ ] 支付功能正常（如已配置）

### 安全检查
- [ ] HTTPS证书有效
- [ ] 数据库密码强度足够
- [ ] JWT密钥已更换
- [ ] 服务器防火墙已配置
- [ ] 敏感端口未对外开放
- [ ] 备份策略已实施

---

## 应急联系

遇到问题时：

1. **查看日志**: `pm2 logs`
2. **检查状态**: `pm2 status`
3. **重启服务**: `pm2 restart snack-api`
4. **回滚代码**: `git checkout <previous-commit>`
5. **联系开发团队**

---

**文档版本**: v1.0  
**最后更新**: 2026-08-12  
**维护者**: 运维团队
