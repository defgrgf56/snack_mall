/* pages/index/index.wxss */
.index-page {
  min-height: 100vh;
  padding-bottom: calc(100rpx + env(safe-area-inset-bottom)); /* TabBar高度 + 安全区 */
  box-sizing: border-box;
}

/* 自定义导航栏 */
.custom-nav-bar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 101;
  background: #FF6B00;
}

.nav-title {
  position: absolute;
  left: 0;
  right: 0;
  font-size: 36rpx;
  font-weight: bold;
  color: #FFFFFF;
  text-align: center;
  letter-spacing: 2rpx;
}

/* 搜索栏 */
.search-bar {
  position: fixed;
  top: calc(88rpx + env(safe-area-inset-top));
  left: 0;
  right: 0;
  z-index: 100;
  background-color: #FF6B00;
  padding: 20rpx;
}

.search-input {
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: #fff;
  border-radius: 50rpx;
  padding: 16rpx 24rpx;
}

.search-icon {
  font-size: 32rpx;
  margin-right: 12rpx;
}

.search-placeholder {
  color: #999;
  font-size: 28rpx;
  text-align: center;
}

/* 轮播图 */
.banner-container {
  margin: 20rpx 20rpx 0;
}

.banner-swiper {
  width: 100%;
  height: 320rpx;
  border-radius: 16rpx;
  overflow: hidden;
}

.banner-image {
  width: 100%;
  height: 100%;
}

/* 分类导航 */
.category-nav {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20rpx;
  padding: 30rpx 20rpx;
  background-color: #fff;
  margin: 20rpx 20rpx 0;
  border-radius: 16rpx;
}

.category-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.category-icon {
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  margin-bottom: 12rpx;
}

.category-name {
  font-size: 24rpx;
  color: #333;
}

/* 优惠券 */
.coupon-section {
  margin: 20rpx 20rpx 0;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 0 20rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #333;
}

.section-more {
  display: flex;
  align-items: center;
  font-size: 24rpx;
  color: #999;
}

.icon-arrow {
  width: 24rpx;
  height: 24rpx;
  margin-left: 8rpx;
}

.coupon-list {
  white-space: nowrap;
}

.coupon-item {
  display: inline-flex;
  width: 560rpx;
  height: 160rpx;
  background: linear-gradient(135deg, #FF6B00 0%, #FF8533 100%);
  border-radius: 16rpx;
  margin-right: 20rpx;
  overflow: hidden;
}

.coupon-left {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 200rpx;
  border-right: 2rpx dashed rgba(255, 255, 255, 0.5);
  color: #fff;
}

.coupon-amount {
  display: flex;
  align-items: baseline;
}

.coupon-symbol {
  font-size: 28rpx;
  margin-right: 4rpx;
}

.coupon-value {
  font-size: 56rpx;
  font-weight: bold;
}

.coupon-condition {
  font-size: 22rpx;
  margin-top: 8rpx;
}

.coupon-right {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 0 24rpx;
  color: #fff;
}

.coupon-name {
  font-size: 28rpx;
  font-weight: bold;
  margin-bottom: 12rpx;
}

.coupon-time {
  font-size: 22rpx;
  opacity: 0.8;
}

/* 商品区域 */
.product-section {
  margin: 20rpx 20rpx 20rpx;
}

.product-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
}

@media screen and (min-width: 768px) {
  .product-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media screen and (min-width: 1024px) {
  .product-grid {
    grid-template-columns: repeat(4, 1fr);
  }
}

.product-item {
  background-color: #fff;
  border-radius: 16rpx;
  overflow: hidden;
}

.product-image {
  width: 100%;
  height: 340rpx;
  background-color: #f5f5f5;
}

.product-tags {
  position: absolute;
  top: 12rpx;
  left: 12rpx;
  display: flex;
  gap: 8rpx;
}

.product-info {
  padding: 16rpx;
}

.product-name {
  font-size: 28rpx;
  color: #333;
  font-weight: bold;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  text-overflow: ellipsis;
  height: 80rpx;
  line-height: 40rpx;
}

.product-subtitle {
  font-size: 22rpx;
  color: #999;
  margin-top: 8rpx;
  display: -webkit-box;
  -webkit-line-clamp: 1;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.product-bottom {
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  margin-top: 16rpx;
}

.product-price {
  display: flex;
  align-items: baseline;
  color: #FF6B00;
}

.price-symbol {
  font-size: 24rpx;
  margin-right: 2rpx;
}

.price-value {
  font-size: 36rpx;
  font-weight: bold;
}

.price-original {
  font-size: 22rpx;
  color: #999;
  text-decoration: line-through;
  margin-left: 8rpx;
}

.product-sales {
  font-size: 22rpx;
  color: #999;
}

/* 购物车按钮 */
.cart-btn {
  width: 56rpx;
  height: 56rpx;
  background: linear-gradient(135deg, #ff9a9e 0%, #ff6b00 100%);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(255, 107, 0, 0.3);
}

.cart-icon {
  font-size: 28rpx;
}

/* 标签 */
.tag {
  padding: 4rpx 12rpx;
  border-radius: 4rpx;
  font-size: 20rpx;
  color: white;
}

.tag-hot {
  background: linear-gradient(135deg, #ff6b00 0%, #ff8533 100%);
}

.tag-new {
  background: linear-gradient(135deg, #1989fa 0%, #38a3ff 100%);
}

/* 空状态 */
.empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 0;
}

.empty-text {
  font-size: 28rpx;
  color: #999;
}

/* 加载中 */
.loading {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 100rpx 0;
  font-size: 28rpx;
  color: #999;
}
