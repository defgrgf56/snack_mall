# 零食小程序商城 - 部署检查清单

## 📋 上线前检查清单

---

## 一、环境准备

### 1.1 服务器环境
- [ ] 购买服务器（阿里云/腾讯云/AWS等）
- [ ] 配置安全组规则（开放80、443、3000端口）
- [ ] 安装 Node.js（v16+）
- [ ] 安装 MySQL（v8.0+）
- [ ] 安装 Nginx
- [ ] 安装 PM2（进程管理）
- [ ] 配置SSL证书（HTTPS）

### 1.2 域名准备
- [ ] 购买域名
- [ ] 域名备案（国内服务器必须）
- [ ] DNS解析配置
  - [ ] api.your-domain.com → 服务器IP
  - [ ] admin.your-domain.com → 服务器IP
- [ ] SSL证书申请和配置

### 1.3 微信小程序
- [ ] 注册微信小程序账号
- [ ] 完成小程序认证（企业）
- [ ] 获取 AppID 和 AppSecret
- [ ] 配置服务器域名白名单
  - [ ] request合法域名: https://api.your-domain.com
  - [ ] uploadFile合法域名: https://api.your-domain.com
  - [ ] downloadFile合法域名: https://api.your-domain.com

---

## 二、数据库部署

### 2.1 MySQL安装
```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install mysql-server

# CentOS/RHEL
sudo yum install mysql-server
```

### 2.2 数据库配置
- [ ] 创建数据库: `snack_mall`
- [ ] 创建数据库用户
- [ ] 设置密码（强密码）
- [ ] 授予权限
- [ ] 导入数据库结构和测试数据

```sql
CREATE DATABASE snack_mall CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'snack_user'@'localhost' IDENTIFIED BY 'your_strong_password';
GRANT ALL PRIVILEGES ON snack_mall.* TO 'snack_user'@'localhost';
FLUSH PRIVILEGES;
```

```bash
# 导入数据
mysql -u snack_user -p snack_mall < database.sql
```

### 2.3 数据库优化
- [ ] 配置 my.cnf 优化参数
- [ ] 创建索引
- [ ] 开启慢查询日志
- [ ] 配置自动备份脚本

---

## 三、后端API部署

### 3.1 代码部署
```bash
# 1. 上传代码到服务器
scp -r 后端API/ user@server:/opt/snack-mall-api/

# 2. 安装依赖
cd /opt/snack-mall-api
npm install --production
```

### 3.2 环境配置
- [ ] 创建 `.env.production` 文件
- [ ] 配置生产环境变量

```env
NODE_ENV=production
PORT=3000

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_NAME=snack_mall
DB_USER=snack_user
DB_PASSWORD=your_strong_password

# JWT配置
JWT_SECRET=your_random_secret_key_change_this
JWT_EXPIRES_IN=7d

# 微信小程序配置
WX_APPID=your_real_appid
WX_SECRET=your_real_secret

# 日志配置
LOG_LEVEL=info
LOG_PATH=/var/log/snack-mall
```

### 3.3 使用PM2启动
```bash
# 安装PM2
npm install -g pm2

# 启动应用
pm2 start src/app.js --name snack-mall-api

# 设置开机自启
pm2 startup
pm2 save

# 查看日志
pm2 logs snack-mall-api

# 监控
pm2 monit
```

### 3.4 检查项
- [ ] API服务正常启动
- [ ] 数据库连接成功
- [ ] 日志正常输出
- [ ] PM2进程稳定运行
- [ ] 内存占用正常

---

## 四、Nginx配置

### 4.1 安装Nginx
```bash
# Ubuntu/Debian
sudo apt-get install nginx

# CentOS/RHEL
sudo yum install nginx
```

### 4.2 配置文件

创建 `/etc/nginx/sites-available/snack-mall`：

```nginx
# API服务器配置
server {
    listen 80;
    server_name api.your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.your-domain.com;
    
    ssl_certificate /path/to/ssl/cert.pem;
    ssl_certificate_key /path/to/ssl/key.pem;
    
    # SSL配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    
    # API代理
    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
    
    # 静态文件
    location /uploads {
        alias /opt/snack-mall-api/uploads;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# 后台管理系统配置
server {
    listen 80;
    server_name admin.your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name admin.your-domain.com;
    
    ssl_certificate /path/to/ssl/cert.pem;
    ssl_certificate_key /path/to/ssl/key.pem;
    
    root /var/www/admin;
    index index.html;
    
    # SPA路由
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Gzip压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

### 4.3 启用配置
```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/snack-mall /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重载配置
sudo systemctl reload nginx

# 设置开机自启
sudo systemctl enable nginx
```

### 4.4 检查项
- [ ] Nginx配置无误
- [ ] SSL证书有效
- [ ] HTTPS访问正常
- [ ] API代理工作正常
- [ ] 静态文件访问正常
- [ ] Gzip压缩生效

---

## 五、小程序部署

### 5.1 代码配置
- [ ] 修改 `utils/config.js` 中的API地址
```javascript
const BASE_URL = 'https://api.your-domain.com/api'
```

- [ ] 修改 `app.json` 中的 AppID
- [ ] 检查所有页面配置
- [ ] 替换TabBar图标（如需要）

### 5.2 微信开发者工具
- [ ] 使用真实AppID
- [ ] 清除缓存
- [ ] 完整测试所有功能
- [ ] 检查网络请求是否成功
- [ ] 检查授权登录流程
- [ ] 检查支付流程（如已开发）

### 5.3 提交审核
- [ ] 点击"上传"按钮
- [ ] 填写版本号和描述
- [ ] 登录小程序后台（mp.weixin.qq.com）
- [ ] 提交审核
- [ ] 填写审核信息
  - [ ] 测试账号
  - [ ] 功能说明
  - [ ] 类目选择
  - [ ] 隐私协议
  - [ ] 用户协议

### 5.4 发布
- [ ] 审核通过
- [ ] 设置体验版（可选）
- [ ] 正式发布

---

## 六、后台管理系统部署

### 6.1 构建
```bash
cd 后台管理系统

# 安装依赖
npm install

# 构建生产版本
npm run build
```

### 6.2 部署
```bash
# 上传到服务器
scp -r dist/* user@server:/var/www/admin/

# 或使用rsync
rsync -avz dist/ user@server:/var/www/admin/
```

### 6.3 检查项
- [ ] 构建无错误
- [ ] 文件上传完整
- [ ] 访问 https://admin.your-domain.com 正常
- [ ] 登录功能正常
- [ ] API请求正常
- [ ] 所有页面可访问

---

## 七、安全配置

### 7.1 服务器安全
- [ ] 配置防火墙（UFW/iptables）
- [ ] 关闭不必要的端口
- [ ] 禁用root登录
- [ ] 配置SSH密钥登录
- [ ] 定期更新系统
- [ ] 安装fail2ban防止暴力破解

### 7.2 数据库安全
- [ ] 使用强密码
- [ ] 限制远程访问
- [ ] 定期备份数据
- [ ] 加密敏感字段
- [ ] 配置数据库审计

### 7.3 应用安全
- [ ] 使用HTTPS
- [ ] 配置CSP策略
- [ ] 防止SQL注入（已使用ORM）
- [ ] 防止XSS攻击
- [ ] 配置CORS
- [ ] 接口限流
- [ ] JWT密钥安全保存

---

## 八、监控和日志

### 8.1 应用监控
- [ ] PM2监控：`pm2 monit`
- [ ] 配置告警通知
- [ ] 设置日志轮转

### 8.2 日志管理
```bash
# PM2日志路径
~/.pm2/logs/

# 应用日志路径
/var/log/snack-mall/

# 配置logrotate
/etc/logrotate.d/snack-mall
```

### 8.3 性能监控
- [ ] 配置APM（如New Relic、阿里云ARMS）
- [ ] 监控服务器资源（CPU、内存、磁盘）
- [ ] 监控数据库性能
- [ ] 监控API响应时间

---

## 九、备份策略

### 9.1 数据库备份
```bash
# 创建备份脚本
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
mysqldump -u snack_user -p snack_mall > /backup/snack_mall_$DATE.sql
# 保留最近7天的备份
find /backup -name "snack_mall_*.sql" -mtime +7 -delete
```

- [ ] 配置每日自动备份
- [ ] 测试恢复流程
- [ ] 异地备份（OSS/S3）

### 9.2 代码备份
- [ ] 使用Git版本控制
- [ ] 定期推送到远程仓库
- [ ] 打标签（tag）记录版本

---

## 十、测试验证

### 10.1 功能测试
- [ ] 用户登录
- [ ] 商品浏览
- [ ] 加入购物车
- [ ] 下单流程
- [ ] 订单管理
- [ ] 地址管理
- [ ] 后台管理

### 10.2 性能测试
- [ ] 并发测试（ab/wrk）
- [ ] 压力测试
- [ ] 响应时间测试

### 10.3 兼容性测试
- [ ] 不同机型测试
- [ ] 不同微信版本测试
- [ ] 网络环境测试（4G/5G/WiFi）

---

## 十一、上线后

### 11.1 监控关注
- [ ] 第一周每天检查日志
- [ ] 关注错误报告
- [ ] 监控用户反馈
- [ ] 跟踪关键指标（UV、订单量等）

### 11.2 持续优化
- [ ] 根据用户反馈优化功能
- [ ] 性能优化
- [ ] 定期更新依赖
- [ ] 安全补丁更新

---

## 📞 应急联系

### 问题处理流程
1. 查看应用日志：`pm2 logs`
2. 查看Nginx日志：`tail -f /var/log/nginx/error.log`
3. 查看数据库日志：`tail -f /var/log/mysql/error.log`
4. 重启服务：`pm2 restart snack-mall-api`
5. 数据库备份恢复：`mysql -u user -p snack_mall < backup.sql`

---

## ✅ 最终检查

在正式上线前，确认所有项目都已勾选完成！

- [ ] 所有环境配置正确
- [ ] 数据库运行正常
- [ ] API服务稳定
- [ ] 小程序审核通过
- [ ] 后台管理可访问
- [ ] 安全配置完成
- [ ] 监控和备份就绪
- [ ] 测试验证通过

---

**部署日期**: ___________  
**部署人员**: ___________  
**版本号**: v1.0.0  

🎉 **祝部署顺利！**
